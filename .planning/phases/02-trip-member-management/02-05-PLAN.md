---
phase: 02-trip-member-management
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/actions/trips.ts
  - src/components/trip/DeleteTripDialog.tsx
  - src/components/trip/EditTripSheet.tsx
  - src/app/t/[tripId]/gente/page.tsx
autonomous: true
requirements: [TRIP-08, TRIP-09]
must_haves:
  truths:
    - "Implements CONTEXT decisions D-14 (edit reuses CreateTripForm pre-filled), D-15 (creator exit = delete-only), D-16 (hard delete only, no archive column), D-17 (type-exact-name to confirm delete)"
    - "The creator can edit the trip name, dates, and description; changes reflect after refresh"
    - "The creator can delete the trip only after typing its exact name to confirm (cascade warning shown)"
    - "Edit and delete affordances are visible only to the creator"
    - "Trip dates render via the es-MX helper wherever they appear"
  artifacts:
    - path: "src/actions/trips.ts"
      provides: "updateTrip + deleteTrip Server Actions (normal RLS, no service-role)"
      contains: "export async function updateTrip"
    - path: "src/components/trip/DeleteTripDialog.tsx"
      provides: "type-name-to-confirm delete (D-17)"
      contains: "AlertDialog"
    - path: "src/components/trip/EditTripSheet.tsx"
      provides: "creator edit surface reusing CreateTripForm pre-filled"
      contains: "CreateTripForm"
  key_links:
    - from: "src/components/trip/EditTripSheet.tsx"
      to: "src/actions/trips.ts updateTrip"
      via: "CreateTripForm onSubmit override → updateTrip → router.refresh()"
      pattern: "updateTrip"
    - from: "src/components/trip/DeleteTripDialog.tsx"
      to: "src/actions/trips.ts deleteTrip"
      via: "type-name-confirm → deleteTrip → redirect"
      pattern: "deleteTrip"
---

<objective>
The trip lifecycle slice: the creator edits name/dates/description by reusing `CreateTripForm` pre-filled (D-14, TRIP-08) and deletes the trip via a GitHub-style type-the-exact-name confirmation inside an `AlertDialog` (D-17, TRIP-09 — hard delete only, D-16). Both `updateTrip` and `deleteTrip` run under the already-shipped creator-only `trips` UPDATE/DELETE RLS policies (normal RLS, no service-role).

Purpose: Completes the trip container's mutation surface. Delete cascades (ON DELETE CASCADE already in schema) — the highest-stakes action — hence type-to-confirm. Edit/delete affordances appear only to the creator.
Output: `updateTrip` + `deleteTrip` in `trips.ts`; `EditTripSheet`; `DeleteTripDialog`; creator-only affordances on the Gente page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trip-member-management/02-RESEARCH.md
@.planning/phases/02-trip-member-management/02-PATTERNS.md
@.planning/phases/02-trip-member-management/02-UI-SPEC.md
@src/components/ui/alert-dialog.tsx

<interfaces>
<!-- Contracts the executor uses directly. -->

From src/actions/trips.ts (02-02 — extend this file; createTrip already there):
- `'use server'`, `const supabase = await createClient()`, `getUser()`, error strings via es.errors.*
- updateTrip/deleteTrip run under NORMAL RLS — do NOT use the service-role admin client (only createTrip does)

From src/components/trip/CreateTripForm.tsx (02-02): accepts optional `defaultValues` + an `onSubmit` override prop → reuse pre-filled for edit
From src/lib/utils/date-format.ts (02-01): `parseLocalDate(s)` to seed the picker from stored 'YYYY-MM-DD'; `formatTripRange` for display; `toLocalDateString` already used inside the form
From src/i18n/es.ts (02-01): `es.trip.{editCta, saveCta, deleteCta, deleteDialogHeading, deleteDialogBody, deleteConfirmLabel(name)}`, `es.errors.genericNetwork`
From src/components/ui/alert-dialog.tsx: base-ui AlertDialog primitive
From src/app/t/[tripId]/gente/page.tsx (02-03): already computes `creatorId` + `currentUserId` + has `trip.name`; add creator-only edit/delete affordances here
From src/types/database.ts — `trips` update columns: name, description (string|null), start_date (string|null), end_date (string|null)
Freshness: `revalidatePath` + `router.refresh()` after update; delete → redirect to `/`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: updateTrip + deleteTrip Server Actions (TRIP-08, TRIP-09)</name>
  <read_first>
    - src/actions/trips.ts (the file being extended — createTrip's imports + getUser pattern; note: edit/delete use the PLAIN client, NOT service-role)
    - .planning/phases/02-trip-member-management/02-PATTERNS.md "trips.ts" updateTrip/deleteTrip note (lines 91-92) — normal RLS, plain client
    - .planning/phases/02-trip-member-management/02-RESEARCH.md Architectural Responsibility Map rows for edit/delete (lines 25-27) + Open Q1 (refetch, no realtime)
  </read_first>
  <action>
    Add to `src/actions/trips.ts`. `export async function updateTrip(tripId: string, input: { name: string; startDate: string | null; endDate: string | null; description: string | null }): Promise<{ error: string | null }>`: `await createClient()`, `getUser()`; `await supabase.from('trips').update({ name: input.name, description: input.description, start_date: input.startDate, end_date: input.endDate }).eq('id', tripId)` — the creator-only UPDATE RLS authorizes; on error return `es.errors.genericNetwork`, else `revalidatePath` the trip routes and return `{ error: null }`. `export async function deleteTrip(tripId: string): Promise<{ error: string | null }>`: `await supabase.from('trips').delete().eq('id', tripId)` — creator-only DELETE RLS + `ON DELETE CASCADE` (already in schema) wipes members/docs/itinerary; on error return `genericNetwork`, else `{ error: null }`. Use the PLAIN SSR client for BOTH — do NOT construct the service-role admin client (only createTrip needs it). Do NOT add an `archived_at` column or any migration (hard delete only — D-16; no new migration this phase).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export async function updateTrip" src/actions/trips.ts && grep -q "export async function deleteTrip" src/actions/trips.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "export async function (updateTrip|deleteTrip)" src/actions/trips.ts` matches both
    - Neither updateTrip nor deleteTrip references `SUPABASE_SECRET_KEY` (`grep -A20 "function updateTrip" src/actions/trips.ts | grep -c SUPABASE_SECRET_KEY` returns 0)
    - updateTrip uses `.update(...).eq('id', tripId)`; deleteTrip uses `.delete().eq('id', tripId)`
    - No new migration added (`ls supabase/migrations/ | grep -c archived` returns 0)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>updateTrip and deleteTrip mutate the trip under the existing creator-only RLS policies via the plain client; delete relies on ON DELETE CASCADE.</done>
</task>

<task type="auto">
  <name>Task 2: EditTripSheet (reuse CreateTripForm pre-filled) + creator-only affordances (D-14, TRIP-08)</name>
  <read_first>
    - src/components/trip/CreateTripForm.tsx (02-02 — defaultValues + onSubmit override props)
    - src/components/ui/sheet.tsx (existing Sheet primitive for the edit surface)
    - src/app/t/[tripId]/gente/page.tsx (02-03 — already has creatorId/currentUserId/trip; add affordances here)
    - .planning/phases/02-trip-member-management/02-UI-SPEC.md §2 form reuse (lines 115-120) + edit-surface placement note
    - .planning/phases/02-trip-member-management/02-PATTERNS.md "CreateTripForm" reuse-for-edit (lines 209-251)
  </read_first>
  <action>
    Create `src/components/trip/EditTripSheet.tsx` ('use client'): a `Sheet` (or trigger button + sheet) labeled `es.trip.editCta` that renders `CreateTripForm` pre-filled from the current trip — `defaultValues` seeded with `name`, `description`, and a `DateRange` reconstructed via `parseLocalDate(start)`/`parseLocalDate(end)` (NEVER `new Date(str)` — Pitfall 2). Pass an `onSubmit` override that calls `await updateTrip(tripId, input)` inside `startTransition`, then on error `toast.error(error)` else `toast.success(...)` + `router.refresh()` (the ProfileNameEditor async-submit-with-toast variant). Submit label is `es.trip.saveCta` ("Guardar") in edit mode. In `src/app/t/[tripId]/gente/page.tsx`, render `<EditTripSheet>` ONLY when `currentUserId === creatorId` (creator-only — the trips UPDATE RLS would deny non-creators anyway, but hide the affordance). Recommended placement (UI-SPEC §2 / RESEARCH Open Q3): an "Editar viaje" affordance atop Gente near the invite card. All strings via es.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "CreateTripForm" src/components/trip/EditTripSheet.tsx && grep -q "updateTrip" src/components/trip/EditTripSheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CreateTripForm" src/components/trip/EditTripSheet.tsx` matches (form reused, not duplicated)
    - `grep -n "updateTrip" src/components/trip/EditTripSheet.tsx` matches via an onSubmit override
    - `grep -c "new Date(" src/components/trip/EditTripSheet.tsx` returns 0 (date seeding uses parseLocalDate)
    - `<EditTripSheet` in gente/page.tsx is gated on `currentUserId === creatorId`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The creator edits the trip via the reused CreateTripForm pre-filled; the affordance is creator-only and changes reflect after refresh.</done>
</task>

<task type="auto">
  <name>Task 3: DeleteTripDialog type-name-to-confirm + creator-only affordance (D-16, D-17, TRIP-09)</name>
  <read_first>
    - src/components/ui/alert-dialog.tsx (base-ui AlertDialog shell)
    - src/components/profile/ProfileNameEditor.tsx (controlled Input pattern for the type-to-confirm gate)
    - .planning/phases/02-trip-member-management/02-UI-SPEC.md §6 Delete trip (lines 147-148)
    - .planning/phases/02-trip-member-management/02-PATTERNS.md "DeleteTripDialog.tsx" (lines 261-263)
  </read_first>
  <action>
    Create `src/components/trip/DeleteTripDialog.tsx` ('use client'): an `AlertDialog` triggered by a destructive `es.trip.deleteCta` affordance, with title `es.trip.deleteDialogHeading`, body `es.trip.deleteDialogBody` (warns of the irreversible cascade), and a controlled `Input` labeled `es.trip.deleteConfirmLabel(name)`. The destructive confirm button (`es.trip.deleteCta`, destructive styling) stays `disabled` until the typed value `=== trip.name` (trim-exact, case-sensitive — D-17). On confirm → `await deleteTrip(tripId)` inside `startTransition`; on error `toast.error(error)`, on success `router.push('/')` (the creator leaves the now-deleted trip; cascade already wiped children). Render `<DeleteTripDialog tripId name={trip.name} />` in `src/app/t/[tripId]/gente/page.tsx` ONLY when `currentUserId === creatorId` (creator-only — this is the creator's sole exit per D-15). Place it as the lowest, visually de-emphasized destructive affordance (e.g. below the member list). All strings via es.ts; no archive option (hard delete only — D-16).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "AlertDialog" src/components/trip/DeleteTripDialog.tsx && grep -q "deleteTrip" src/components/trip/DeleteTripDialog.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AlertDialog" src/components/trip/DeleteTripDialog.tsx` matches
    - `grep -n "deleteConfirmLabel" src/components/trip/DeleteTripDialog.tsx` matches (type-to-confirm label)
    - The confirm button is disabled until the typed value equals the trip name (controlled Input gate present)
    - `grep -n "deleteTrip" src/components/trip/DeleteTripDialog.tsx` matches and success path redirects to `/`
    - `<DeleteTripDialog` in gente/page.tsx is gated on `currentUserId === creatorId`
    - No "archivar" / archive affordance present (hard delete only)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The creator can delete the trip only after typing its exact name; the cascade warning is shown; the affordance is creator-only and there is no archive path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → updateTrip/deleteTrip | Untrusted tripId + edit payload cross into the server |
| server → Supabase | Creator-only mutation authorized by RLS, not app-layer checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Elevation of Privilege | non-creator forging updateTrip/deleteTrip | mitigate | `trips` UPDATE/DELETE RLS policies are creator-only; a forged call by a non-creator is denied at the DB regardless of the hidden UI affordance; plain SSR client (no service-role bypass) |
| T-02-16 | Tampering | accidental destructive delete | mitigate | Type-the-exact-name AlertDialog gate (D-17) before deleteTrip — the highest-stakes action; confirm button disabled until exact match |
| T-02-17 | Spoofing | actor identity on edit/delete | mitigate | Actor from server `getUser()`; tripId is only a row selector, RLS binds the mutation to the creator |
| T-02-18 | Information Disclosure | cascade scope | accept | `ON DELETE CASCADE` (already in schema) intentionally wipes the trip's members/docs/itinerary; bounded to the single deleted trip id |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run build` exits 0
- updateTrip/deleteTrip use the plain SSR client (no service-role)
- Edit + delete affordances are creator-gated
- No new migration / no archived_at column
- Device UAT (phase gate): creator edits name/dates (reflected after refresh); delete requires exact-name typing and cascades, redirecting to /
</verification>

<success_criteria>
- ROADMAP SC4: creator edits name/dates/description and changes are reflected (refetch model — RESEARCH Open Q1, no realtime in Phase 2)
- ROADMAP SC5: trip dates render via the es-MX helper across views (helper consumed here + in 02-03)
- TRIP-08: creator edits trip; TRIP-09: creator deletes (hard delete, type-to-confirm)
</success_criteria>

<output>
Create `.planning/phases/02-trip-member-management/02-05-SUMMARY.md` when done
</output>

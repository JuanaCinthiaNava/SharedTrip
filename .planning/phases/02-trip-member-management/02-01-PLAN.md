---
phase: 02-trip-member-management
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/lib/utils/invite-code.ts
  - src/lib/utils/invite-code.test.ts
  - src/lib/utils/date-format.ts
  - src/lib/utils/date-format.test.ts
  - src/i18n/es.ts
  - src/i18n/es.test.ts
  - src/components/ui/calendar.tsx
  - src/components/ui/textarea.tsx
  - package.json
autonomous: true
requirements: [TRIP-02, UI-05]
must_haves:
  truths:
    - "Implements CONTEXT decisions D-06 (server-generated invite_code: name prefix + unambiguous suffix, retry on 23505) and D-18 (shared timezone-safe es-MX date helper)"
    - "generateInviteCode always produces a CODE_RE-valid string with an unambiguous suffix"
    - "Trip dates format in es-MX without an off-by-one day shift"
    - "All new Spanish strings live in es.ts (trip / members / invite namespaces)"
  artifacts:
    - path: "src/lib/utils/invite-code.ts"
      provides: "generateInviteCode(name) generator"
      contains: "export function generateInviteCode"
    - path: "src/lib/utils/date-format.ts"
      provides: "es-MX date helpers (parseLocalDate, toLocalDateString, formatTripRange)"
      contains: "Intl.DateTimeFormat('es-MX'"
    - path: "src/i18n/es.ts"
      provides: "trip / members / invite Spanish namespaces"
      contains: "shareMessage"
    - path: "src/components/ui/calendar.tsx"
      provides: "shadcn base-nova Calendar primitive (react-day-picker)"
    - path: "src/components/ui/textarea.tsx"
      provides: "shadcn base-nova Textarea primitive"
  key_links:
    - from: "src/lib/utils/invite-code.ts"
      to: "CODE_RE"
      via: "generated code must pass the existing regex"
      pattern: "CODE_RE"
---

<objective>
Ship the cross-cutting foundation every Phase 2 slice consumes: the server-side `invite_code` generator (D-06, TRIP-02), the single-source-of-truth es-MX date helper (D-18, UI-05), all new Spanish strings (`trip`/`members`/`invite` namespaces), and the two new shadcn primitives (`calendar`, `textarea`) installed via the base-nova CLI.

Purpose: These are pure utilities + dictionary + primitives with no UI of their own. Isolating them in Wave 0 lets every downstream slice import stable, unit-tested contracts and avoids es.ts merge conflicts across parallel plans.
Output: `generateInviteCode`, `date-format.ts` helpers, extended es.ts + es.test.ts, `calendar.tsx`, `textarea.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trip-member-management/02-RESEARCH.md
@.planning/phases/02-trip-member-management/02-PATTERNS.md
@.planning/phases/02-trip-member-management/02-UI-SPEC.md

<interfaces>
<!-- Existing contracts the executor must honor. Do NOT explore the codebase for these. -->

From src/lib/utils/invite-code.ts:
- `export const CODE_RE = /^[A-Za-z0-9]{2,8}-[A-Za-z0-9]{3,6}$/` (prefix 2–8, suffix 3–6)
- `export function normalizeInviteCode(raw: string): string` (trim + toUpperCase)
- generated code must pass CODE_RE AND round-trip unchanged through normalizeInviteCode (already uppercase)

From src/i18n/es.ts — existing top-level namespaces (append after these, inside the `as const` object):
`entry`, `auth`, `tabs`, `profile`, `tripSwitcher`, `anon`, `errors`
Function members are already an established pattern (e.g. `auth.checkEmailBody`, `anon.upgradeSuccessToast`).

From src/lib/utils/avatar.ts (analog for a pure util module): top-of-file comment warning tests pin behavior; named `export function`s; no React import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generateInviteCode generator + tests (TDD, D-06 / TRIP-02)</name>
  <read_first>
    - src/lib/utils/invite-code.ts (the file being modified — CODE_RE + normalizeInviteCode contracts)
    - src/lib/utils/invite-code.test.ts (test structure to extend — vitest, node, pure-fn)
    - .planning/phases/02-trip-member-management/02-RESEARCH.md Pattern 2 (lines 241-272) — exact generator shape + padding branch + Pitfall 3
    - .planning/phases/02-trip-member-management/02-PATTERNS.md section "invite-code.ts — ADD generateInviteCode" (lines 147-176)
  </read_first>
  <behavior>
    - Test: generateInviteCode('Cancún 2026') run 500× → every result matches CODE_RE
    - Test: suffix (chars after the '-') never contains any of O, I, L, 0, 1 (unambiguous alphabet)
    - Test: a 1-character / non-alpha name (e.g. '7', '🌴', ' ') still yields a CODE_RE-valid code (prefix padded to ≥ 2)
    - Test: prefix strips non-alpha and uppercases (e.g. 'café 2026' → prefix 'CAFE'); prefix length ≤ 4 alpha chars
    - Test: output is uppercase → normalizeInviteCode(generateInviteCode(x)) === generateInviteCode(x)
  </behavior>
  <action>
    Add `export function generateInviteCode(name: string): string` to `src/lib/utils/invite-code.ts` per RESEARCH Pattern 2. Prefix = `name.toUpperCase().replace(/[^A-Z]/g,'').slice(0,4)`; if that yields < 2 chars, pad from the suffix alphabet to length 2 so CODE_RE always matches. Suffix = 4 chars drawn from the unambiguous alphabet constant `SUFFIX_ALPHABET = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789'` (excludes O/I/L/0/1 per D-06). Return `` `${prefix}-${suffix}` ``. Use `Math.random()` (RESEARCH A1: this is a display capability code, not a secret). Do NOT alter CODE_RE, normalizeInviteCode, or isWellFormedInviteCode. Extend `src/lib/utils/invite-code.test.ts` with a `describe('generateInviteCode')` block covering all five behaviors above (copy the import + describe shape from the existing file).
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils/invite-code.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/utils/invite-code.test.ts` exits 0 with the new generateInviteCode cases passing
    - `grep -n "export function generateInviteCode" src/lib/utils/invite-code.ts` returns a match
    - The 500-iteration CODE_RE test and the O/I/L/0/1 exclusion test are both present and green
  </acceptance_criteria>
  <done>generateInviteCode exists, always emits a CODE_RE-valid unambiguous code, and is covered by passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: es-MX date-format helper + tests (TDD, D-18 / UI-05)</name>
  <read_first>
    - src/lib/utils/avatar.ts (analog: pure deterministic util module shape + module-level comment)
    - src/lib/utils/avatar.test.ts (analog: vitest node fixed-input test style)
    - .planning/phases/02-trip-member-management/02-RESEARCH.md Patterns 4 & 5 (lines 307-340) + Pitfall 2 (lines 447-451) — verified es-MX outputs and the UTC off-by-one trap
    - .planning/phases/02-trip-member-management/02-PATTERNS.md section "date-format.ts" (lines 180-205)
  </read_first>
  <behavior>
    - Test: toLocalDateString(parseLocalDate('2026-06-05')) === '2026-06-05' (NO UTC day shift — the core Pitfall 2 guard)
    - Test: formatTripRange('2026-06-05', null) returns a single-date es-MX string containing '5' and 'jun' (no off-by-one to 4)
    - Test: formatTripRange('2026-06-05', '2026-06-12') returns a range string (one value, both endpoints in es-MX)
    - Test: formatTripRange('2026-06-28', '2026-07-03') cross-month range renders both month names
    - Test: formatTripRange('2026-12-30', '2027-01-02') cross-year range renders both years
    - Test: formatTripRange(null, null) === null (caller hides the date row — D-03)
  </behavior>
  <action>
    Create `src/lib/utils/date-format.ts` per RESEARCH Patterns 4 & 5. Export `toLocalDateString(d: Date): string` (build from `getFullYear/getMonth/getDate` — NEVER `.toISOString()`), `parseLocalDate(s: string): Date` (split 'YYYY-MM-DD' and construct `new Date(y, m-1, d)` — NEVER `new Date(str)`), and `formatTripRange(start: string | null, end: string | null): string | null` backed by a module-level `const FMT = new Intl.DateTimeFormat('es-MX', { day: 'numeric', month: 'short', year: 'numeric' })`. Lock ONE range style: use `FMT.formatRange(parseLocalDate(start), parseLocalDate(end))` when both present, `FMT.format(parseLocalDate(one))` for a single date, `null` when both null. Add a top-of-file comment (mirror avatar.ts) stating: single source of truth for es-MX dates; never `.toISOString()`/`new Date(str)` (Pitfall 2). Create `src/lib/utils/date-format.test.ts` covering all six behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils/date-format.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/utils/date-format.test.ts` exits 0
    - `grep -n "Intl.DateTimeFormat('es-MX'" src/lib/utils/date-format.ts` returns a match
    - `grep -nE "toISOString|new Date\([a-z]" src/lib/utils/date-format.ts | grep -v '^#'` returns NOTHING (no UTC-unsafe date construction)
    - The round-trip test (`2026-06-05` survives parse→serialize) is present and green
  </acceptance_criteria>
  <done>date-format.ts exports the three helpers, produces correct es-MX output with no day shift, and is fully unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: es.ts namespaces + es.test.ts + shadcn calendar & textarea primitives (TRIP-02, UI-05)</name>
  <read_first>
    - src/i18n/es.ts (the file being modified — `as const` typed dictionary; existing namespaces + errors keys)
    - src/i18n/es.test.ts (test that asserts namespace key presence — extend it)
    - .planning/phases/02-trip-member-management/02-RESEARCH.md es.ts additions (lines 361-401) — the EXACT entry bodies to copy
    - .planning/phases/02-trip-member-management/02-UI-SPEC.md Copywriting Contract (lines 169-187) — string-to-surface mapping
    - .planning/phases/02-trip-member-management/02-PATTERNS.md Pitfall 4 note (base-ui/base-nova; install via CLI, never hand-copy)
    - components.json (confirms base-nova preset for the CLI add)
  </read_first>
  <action>
    Append three namespaces — `trip`, `members`, `invite` — inside the `as const` object in `src/i18n/es.ts`, copying the EXACT bodies from RESEARCH lines 364-401 (including function members `deleteConfirmLabel(name)`, `removeDialogHeading(name)`, `leaveDialogHeading(trip)`, `shareMessage(name, code, origin)`). Do not invent new keys or alter wording; the bodies are the locked copy contract. Then extend `src/i18n/es.test.ts` with `describe` blocks asserting presence of the new `trip`/`members`/`invite` keys (mirror the existing namespace-key assertions) to guard against accidental renames. Finally install the two missing primitives via the base-nova CLI: run `npx shadcn@latest add calendar` (pulls react-day-picker 10.x + date-fns) and `npx shadcn@latest add textarea`. Do NOT hand-author either component (Pitfall 4 — this repo is base-ui/base-nova, not Radix); accept whatever the CLI generates. Confirm `src/components/ui/calendar.tsx` and `src/components/ui/textarea.tsx` now exist.
  </action>
  <verify>
    <automated>npx vitest run src/i18n/es.test.ts && test -f src/components/ui/calendar.tsx && test -f src/components/ui/textarea.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/i18n/es.test.ts` exits 0 with new trip/members/invite key assertions
    - `grep -nE "shareMessage|deleteConfirmLabel|removeDialogHeading|leaveDialogHeading" src/i18n/es.ts` returns all four function members
    - `src/components/ui/calendar.tsx` and `src/components/ui/textarea.tsx` both exist
    - `grep -n "react-day-picker" package.json` returns a match (installed transitively by the calendar add)
    - `npx tsc --noEmit` exits 0
    - No `@radix-ui/*` import appears in calendar.tsx or textarea.tsx (`grep -L "@radix-ui" src/components/ui/calendar.tsx` succeeds)
  </acceptance_criteria>
  <done>The trip/members/invite namespaces exist with the exact locked copy, es.test.ts guards them, and the base-nova calendar + textarea primitives are installed via CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | New transitive deps (react-day-picker, date-fns) enter via `npx shadcn add` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | invite_code generation | mitigate | Generated code is a display capability token, not a secret; unambiguous-alphabet entropy + UNIQUE constraint (enforced in 02-02). No secret material in this plan. |
| T-02-SC | Tampering | `npx shadcn add` (react-day-picker, date-fns) | accept | Sourced from the authoritative shadcn registry; react-day-picker has ~11-yr history, ~39M weekly downloads, real repo `github.com/gpbl/react-day-picker` (RESEARCH Package Legitimacy Audit — Approved, not [ASSUMED]). Install via CLI resolves the exact shadcn-expected version; no bare guessed-version install. |
</threat_model>

<verification>
- `npm test` (full suite) green after this plan
- `npx tsc --noEmit` exits 0
- No UTC-unsafe date construction anywhere in date-format.ts
- No hardcoded Spanish introduced (strings live only in es.ts)
</verification>

<success_criteria>
- generateInviteCode produces only CODE_RE-valid, unambiguous codes (TRIP-02 generation half)
- es-MX date helper renders correctly with no off-by-one (UI-05)
- trip/members/invite namespaces present with locked copy
- calendar + textarea primitives installed via base-nova CLI
</success_criteria>

<output>
Create `.planning/phases/02-trip-member-management/02-01-SUMMARY.md` when done
</output>

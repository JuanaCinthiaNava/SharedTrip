---
phase: 02-trip-member-management
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/app/t/[tripId]/gente/page.tsx
  - src/components/members/MemberList.tsx
  - src/components/members/MemberRow.tsx
  - src/components/members/InviteCard.tsx
  - src/components/layout/TripSwitcherSheet.tsx
autonomous: true
requirements: [TRIP-02, TRIP-04, TRIP-05]
must_haves:
  truths:
    - "Every member sees the member list with avatars, names, and role badges (Creador / Tú)"
    - "The invite card shows the trip's code and copies a friendly Spanish invitation (deep-link + bare code) to the clipboard"
    - "The invite card doubles as the empty state when the creator is the only member"
    - "A user can switch trips and reach the create flow from the trip switcher"
  artifacts:
    - path: "src/app/t/[tripId]/gente/page.tsx"
      provides: "RSC member-list page (trip_members JOIN profiles, RLS-gated)"
      contains: "trip_members"
    - path: "src/components/members/MemberRow.tsx"
      provides: "avatar + name + role badge row"
      contains: "UserAvatar"
    - path: "src/components/members/InviteCard.tsx"
      provides: "code display + Copiar invitación (clipboard)"
      contains: "writeText"
    - path: "src/components/layout/TripSwitcherSheet.tsx"
      provides: "wired Crear nuevo viaje entry"
  key_links:
    - from: "src/app/t/[tripId]/gente/page.tsx"
      to: "trip_members"
      via: "RSC select JOIN profiles, member-gated SELECT RLS"
      pattern: "trip_members"
    - from: "src/components/members/InviteCard.tsx"
      to: "navigator.clipboard"
      via: "shareMessage copy"
      pattern: "clipboard.writeText"
    - from: "src/components/layout/TripSwitcherSheet.tsx"
      to: "create flow"
      via: "Crear nuevo viaje wired"
      pattern: "createCta"
---

<objective>
The share-and-see slice: the Gente tab becomes a real member list (avatars + names + role badges — D-10) topped by a pinned invite card that displays the code and copies a friendly Spanish invitation (D-07/08/09), and the trip-switcher's disabled "Crear nuevo viaje" button is wired (D-05). After this slice a creator can share their trip and any member can see who's in it (TRIP-02 sharing half, TRIP-04, TRIP-05).

Purpose: Closes the create→share→see loop — a created trip is now invitable and its membership visible. The invite card doubles as the sole-member empty state.
Output: replaced `gente/page.tsx`, `MemberList`, `MemberRow`, `InviteCard`, wired `TripSwitcherSheet`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trip-member-management/02-RESEARCH.md
@.planning/phases/02-trip-member-management/02-PATTERNS.md
@.planning/phases/02-trip-member-management/02-UI-SPEC.md
@src/app/t/[tripId]/layout.tsx
@src/app/t/[tripId]/gente/page.tsx
@src/components/layout/TripSwitcherSheet.tsx
@src/components/profile/UserAvatar.tsx

<interfaces>
<!-- Contracts the executor uses directly. -->

From src/app/t/[tripId]/layout.tsx (RSC-fetch-under-RLS analog):
- `const supabase = await createClient()` → `const { data: { user } } = await supabase.auth.getUser()`
- nested-relation select + flatten pattern (layout.tsx:42-51)
- to read members: `.from('trip_members').select('user_id, role, profiles(display_name, avatar_seed)').eq('trip_id', tripId)` (member-gated SELECT RLS hides non-member trips)
- to read the invite code + creator: `.from('trips').select('name, invite_code, created_by').eq('id', tripId).single()`

From src/components/profile/UserAvatar.tsx: `<UserAvatar userId avatarSeed displayName size="md" />`
From src/i18n/es.ts (02-01): `es.members.{heading, badgeCreator, badgeYou}`, `es.invite.{cardHeading, cardBody, copyCta, copiedToast, shareMessage(name, code, origin)}`, `es.errors.genericNetwork`
From src/lib/utils/date-format.ts (02-01): `formatTripRange(start, end)` for any date display
From src/components/ui/sonner.tsx + sonner `toast`: `toast.success` / `toast.error`
From src/components/layout/TripSwitcherSheet.tsx: the disabled `<button ... disabled aria-label={es.tripSwitcher.createCta}>` to replace (lines 64-72); existing trip-row `<Link>` style
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace gente/page.tsx with the RSC member-list page (TRIP-05, TRIP-02 display)</name>
  <read_first>
    - src/app/t/[tripId]/gente/page.tsx (current EmptyState placeholder to replace)
    - src/app/t/[tripId]/layout.tsx (RSC fetch + flatten pattern, lines 21-58)
    - .planning/phases/02-trip-member-management/02-PATTERNS.md section "gente/page.tsx" (lines 267-281)
    - .planning/phases/02-trip-member-management/02-UI-SPEC.md §4 Member list (lines 129-134) + §5 Invite card (lines 136-141)
  </read_first>
  <action>
    Replace `src/app/t/[tripId]/gente/page.tsx` with an RSC that: `await createClient()`, `getUser()`, fetch the trip (`name, invite_code, created_by`) and the members (`trip_members.select('user_id, role, profiles(display_name, avatar_seed)').eq('trip_id', tripId)`), flatten the nested `profiles` relation (mirror layout.tsx:48-51). Compute `currentUserId = user.id` and `creatorId = trip.created_by`. Render `<InviteCard name={trip.name} code={trip.invite_code} />` pinned at top (D-07), then — only when there is ≥1 member beyond the current user — a "Miembros del viaje" heading (`es.members.heading`, 20px/700) and `<MemberList members={...} currentUserId={...} creatorId={...} tripId={tripId} tripName={trip.name} />`. When the user is the sole member, the invite card alone IS the empty state (no separate empty screen — D-07). All strings via es.ts. Use page horizontal padding `px-4` and section gap `gap-8` per UI-SPEC spacing.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "trip_members" src/app/t/[tripId]/gente/page.tsx && grep -q "InviteCard" src/app/t/[tripId]/gente/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "trip_members" src/app/t/[tripId]/gente/page.tsx` matches and selects `profiles(display_name, avatar_seed)`
    - `grep -n "created_by" src/app/t/[tripId]/gente/page.tsx` matches (creator detection)
    - `<InviteCard` rendered above the member list; old `EmptyState` placeholder removed
    - No raw SQL / service-role client (RSC read runs under normal member-gated RLS)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The Gente tab fetches members under RLS and renders the invite card + member list (or invite-card-as-empty-state).</done>
</task>

<task type="auto">
  <name>Task 2: MemberList + MemberRow (D-10, TRIP-05)</name>
  <read_first>
    - src/components/profile/UserAvatar.tsx (avatar+name composition)
    - .planning/phases/02-trip-member-management/02-PATTERNS.md sections "MemberRow.tsx" (lines 285-293) + "MemberList.tsx" (line 25)
    - .planning/phases/02-trip-member-management/02-UI-SPEC.md §4 (lines 129-134) + Color role-badge spec (lines 97-101) + Typography (lines 57-66)
  </read_first>
  <action>
    Create `src/components/members/MemberList.tsx` (may be an RSC — it just maps rows) that maps `members` to `<MemberRow>`, passing `currentUserId`, `creatorId`, `tripId`, `tripName` through. Create `src/components/members/MemberRow.tsx` rendering, horizontally (`gap-4`, `min-h-11`, `py-3`): `<UserAvatar userId={m.user_id} avatarSeed={m.profiles?.avatar_seed} displayName={m.profiles?.display_name} size="md" />` · display name (16px/400 `text-fg`) · role badge(s) · a right-aligned slot for the inline action (left as a placeholder/prop in THIS plan — 02-04 wires remove/leave into it). Badges per UI-SPEC: `Creador` (shown when `m.user_id === creatorId`) = coral-tinted pill `bg-primary/15 text-primary` 14px/700 `rounded-md px-2 py-1` (`es.members.badgeCreator`); `Tú` (shown when `m.user_id === currentUserId`) = neutral pill `bg-surface text-fg-muted border border-border` (`es.members.badgeYou`); a user who is both shows BOTH (`Creador` first, `Tú` second, `gap-2`). Never apply coral/teal to the row body itself (Color contract). NEVER reserve the action slot for the creator's own row to show "Salir" (creator exit = delete only — D-15); leave the slot empty for now and let 02-04 populate it. All strings via es.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "UserAvatar" src/components/members/MemberRow.tsx && grep -q "badgeCreator" src/components/members/MemberRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "UserAvatar" src/components/members/MemberRow.tsx` matches with `size="md"`
    - `grep -nE "badgeCreator|badgeYou" src/components/members/MemberRow.tsx` matches both
    - Creator badge uses `bg-primary/15 text-primary`; Tú badge uses neutral `bg-surface ... border-border` (no coral)
    - `MemberRow` exposes a right-aligned action slot (prop or children) for 02-04 to fill
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Member rows render avatar + name + correct role badge(s) with an action slot reserved for 02-04.</done>
</task>

<task type="auto">
  <name>Task 3: InviteCard clipboard copy (D-07/08/09, TRIP-02) + wire TripSwitcher create button (D-05, TRIP-04)</name>
  <read_first>
    - src/components/auth/InviteCodeForm.tsx ('use client' + sonner wiring analog)
    - src/components/layout/TripSwitcherSheet.tsx (the disabled create button to wire, lines 63-72)
    - .planning/phases/02-trip-member-management/02-RESEARCH.md Pattern 6 (lines 342-359) — clipboard + secure-context note
    - .planning/phases/02-trip-member-management/02-UI-SPEC.md §5 Invite card (lines 136-141) + Empty states (lines 157-163)
  </read_first>
  <action>
    Create `src/components/members/InviteCard.tsx` ('use client') per RESEARCH Pattern 6. Card: `bg-surface rounded-lg p-6` with `gap-4` internal stack — title `es.invite.cardHeading` (20px/700), helper body `es.invite.cardBody` (14–16px/400 `text-fg-muted`), the code value `{code}` displayed prominently (20px/700 `text-accent tracking-wide tabular-nums` — no new mono font), and a full-width coral `Button` `es.invite.copyCta`. On click: build `origin` from `window.location.origin`, call `await navigator.clipboard.writeText(es.invite.shareMessage(name, code, origin))`; on success `toast.success(es.invite.copiedToast)`, on catch `toast.error(es.errors.genericNetwork)` (clipboard blocked / insecure context). The payload carries BOTH the `/join/[code]` deep-link AND the bare code (D-09). Then wire `src/components/layout/TripSwitcherSheet.tsx`: replace the `disabled` `<button>` (lines 64-72) with an enabled control that triggers the create flow (navigate to the create surface / `/trips/new` entry from 02-02), keeping `es.tripSwitcher.createCta` and the existing row styling. Keep the no-trips empty-state copy (`es.tripSwitcher.emptyHeading/emptyBody`) unchanged (TRIP-04 list already renders).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "clipboard.writeText" src/components/members/InviteCard.tsx && ! grep -q "disabled" src/components/layout/TripSwitcherSheet.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "clipboard.writeText" src/components/members/InviteCard.tsx` matches and uses `es.invite.shareMessage`
    - `grep -n "copiedToast" src/components/members/InviteCard.tsx` matches (success toast)
    - The code value renders with `text-accent` (teal emphasis per Color contract)
    - `grep -c "disabled" src/components/layout/TripSwitcherSheet.tsx` returns 0 (create button enabled and wired)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The invite card copies a friendly Spanish invitation (deep-link + bare code) with a success toast, and the trip-switcher create button reaches the create flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → RSC | Member-list read crosses the RLS boundary (only members see the trip) |
| browser → clipboard | Copy requires a secure context (HTTPS) + user gesture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | gente member-list read | mitigate | Read runs under normal member-gated SELECT RLS via the plain SSR client (`is_trip_member()`); a non-member cannot fetch the member list or invite_code |
| T-02-08 | Information Disclosure | invite_code surfaced in UI | accept | Code is a display capability token shown only to authenticated members on a member-gated page; possession-equals-capability (matches shipped resolver model) |
| T-02-09 | Information Disclosure | clipboard share message | mitigate | Copy uses `navigator.clipboard.writeText` (secure-context API, prod HTTPS); no trip id enumeration oracle — the message contains only this trip's already-known code + deep-link |
| T-02-10 | Spoofing | TripSwitcher create wiring | mitigate | Create entry routes to the 02-02 flow where identity comes from the server session; the switcher passes no identity |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run build` exits 0
- Member list reads under RLS (no service-role client in this plan)
- TripSwitcher create button no longer disabled
- Device UAT (phase gate): member sees the list with correct badges; copy yields a "Copiado" toast and a paste containing both the deep-link and the bare code
</verification>

<success_criteria>
- ROADMAP SC2: creator shares the invite code; the copied message works for both Phase 1 entry paths (deep-link + typed code)
- ROADMAP SC3 (read half): all members see the member list with names and avatars/initials and role badges
- TRIP-04: trip list/switch reachable; create entry wired
</success_criteria>

<output>
Create `.planning/phases/02-trip-member-management/02-03-SUMMARY.md` when done
</output>

---
phase: 1
slug: foundation-auth
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-29
revised: 2026-05-29
---

# Phase 1 — UI Design Contract: Foundation + Auth

> Visual and interaction contract for Phase 1. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> All values pre-populated from CONTEXT.md decisions (D-05 through D-17). No open questions remain.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui (v4-compatible) | CONTEXT.md D-05 / CLAUDE.md |
| Preset | none — greenfield; run `npx shadcn@latest init` with Tailwind v4 during Phase 1 bootstrap | decision |
| Component library | Radix UI (via shadcn) | CLAUDE.md stack |
| Icon library | Lucide React (ships with shadcn default) | CLAUDE.md / default |
| Font | Inter (variable), self-hosted via `next/font/google`, `display: 'swap'` | CONTEXT.md D-07 |

**shadcn initialization note:** Phase 1 bootstrap MUST run `npx shadcn@latest init` which will auto-detect Tailwind v4. All `@theme` color tokens are defined in `globals.css` per the Tailwind v4 CSS-first pattern — NOT in a `tailwind.config.js`. The shadcn `components.json` will point to the project's Tailwind v4 setup.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, tab-bar icon-to-label gap, pill horizontal padding |
| sm | 8px | Input internal padding, avatar-to-name gap, badge padding |
| md | 16px | Default element spacing, card body padding, form field gap |
| lg | 24px | Section padding, header top/bottom padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Full-bleed screen vertical padding top |
| 3xl | 64px | Bottom clearance above tab bar (content must not be obscured) |

Exceptions:
- Touch targets for tab bar items: minimum 44px height (iOS HIG compliance) — this exceeds the xl token but is a separate constraint.
- Bottom tab bar height: 56px fixed (contains 44px touch target + 12px visual padding).
- "Sin cuenta" pill height: 24px (sm vertical padding inside a rounded-full badge).

---

## Typography

Single typeface: **Inter variable**, loaded via `next/font/google`.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Display | 28px | 700 (bold) | 1.2 | Screen headings (welcome screen title, empty-state heading) |
| Heading | 20px | 700 (bold) | 1.3 | Section titles, modal/sheet titles, card titles |
| Body | 16px | 400 (regular) | 1.5 | Primary readable text — descriptions, form help text, banner copy |
| Label | 14px | 400 (regular) | 1.4 | Input labels, tab bar labels, badge text, secondary metadata |

**Constraints:**
- Maximum 4 sizes declared above. No additional sizes may be introduced in Phase 1.
- Maximum 2 weights in use: 400 (regular) and 700 (bold). No intermediate weights (500, 600) permitted.
- Brand wordmark "SharedTrip": Inter italic, weight 700, `--color-primary` (coral). Rendered as text, not SVG.
- Tab bar labels: 14px Inter weight 400 (Label token). No sub-token exceptions permitted.

---

## Color

**Theme: Tropical Sunset, dark-mode default. No light-mode toggle in Phase 1.**

All colors defined as Tailwind v4 `@theme` tokens in `globals.css`. No hex literals in component files — reference tokens only.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `--color-bg` | `#0F1729` | Page background — every screen's root background |
| Secondary (30%) | `--color-surface` | `#1A2238` | Cards, sheets, bottom sheet backgrounds, tab bar background, input background |
| Accent primary (10%) | `--color-primary` | `#FF6B6B` | See reserved list below |
| Accent secondary | `--color-accent` | `#3DCCC7` | See reserved list below |
| Warning / notice | `--color-secondary` | `#FFB627` | "Sin cuenta" pill background, nudge banner background highlight |
| Foreground primary | `--color-fg` | `#FAFAFA` | All primary text |
| Foreground muted | `--color-fg-muted` | `#94A3B8` | Secondary text, inactive tab labels, placeholder text, footer copy |
| Destructive | `--color-destructive` | `#EF4444` | Sign-out destructive confirmation only |

**Accent primary (`--color-primary` coral) reserved for:**
1. Primary CTA buttons ("Entrar", "Continuar", "Enviar enlace")
2. Magic link email CTA button background
3. Active tab bar indicator (underline + icon tint)
4. Brand wordmark "SharedTrip" text color
5. PWA icon background (coral square with white "S")

**Accent secondary (`--color-accent` teal) reserved for:**
1. Inline text links
2. Success toast background
3. "Perfil" tab active state (alternate to coral — only if visual conflict with coral active tab arises; default is coral for all active tabs)

**Warning (`--color-secondary` mango) reserved for:**
1. "Sin cuenta" pill badge (anonymous session indicator)
2. Persistent upgrade nudge banner left border/accent stripe
3. Toast notifications for non-error warnings

**Nothing else** may use accent colors. Default interactive states use `--color-fg` on `--color-surface`.

---

## Component Inventory (Phase 1 scope)

Screens and components this phase ships. All use shadcn primitives unless noted.

### Screens

| Screen | Route | Description |
|--------|-------|-------------|
| Welcome / Auth | `/` | Magic link request form. Unauthenticated landing. |
| Magic link sent | `/auth/check-email` | Confirmation screen after form submit. |
| Auth callback | `/auth/callback` | Server route — no visual component. |
| Join via invite | `/join/[token]` | Anonymous join handler — renders trip view or error. |
| Trip shell | `/t/[tripId]/(tabs)` | Bottom-tab shell with 4 placeholder tabs. |
| Perfil tab | `/t/[tripId]/perfil` | Display name edit + sign-out. |
| Gente tab (placeholder) | `/t/[tripId]/gente` | Empty state — Phase 2 fills content. |
| Docs tab (placeholder) | `/t/[tripId]/docs` | Empty state — Phase 3 fills content. |
| Itinerario tab (placeholder) | `/t/[tripId]/itin` | Empty state — Phase 4 fills content. |

**Welcome screen focal point:** The full-width coral "Enviar enlace de acceso" button anchors the bottom third of the Welcome screen above the fold. It is the only coral element on the screen and must remain unobscured by the soft keyboard on mobile — implement with `position: sticky; bottom: 24px` inside a flex column layout so the button stays in the lower viewport third when content is short.

### Components

| Component | shadcn primitive | Notes |
|-----------|-----------------|-------|
| `BottomTabBar` | custom (no shadcn equivalent) | Fixed bottom, 56px height, 4 tabs, coral active indicator |
| `TopHeader` | custom | Trip name dropdown trigger left, avatar + "Sin cuenta" pill right |
| `TripSwitcherSheet` | `Sheet` (shadcn) | Slides up from bottom — lists user trips + "+ Crear nuevo viaje" empty entry |
| `MagicLinkForm` | `Form` + `Input` + `Button` (shadcn) | Email input + submit; RHF + Zod validation |
| `AnonymousBanner` | custom | Persistent dismissible banner below header; mango accent stripe |
| `AnonymousUpgradeSheet` | `Sheet` (shadcn) | Email-only upgrade form; confirms via toast |
| `UserAvatar` | custom | Colored circle + animal emoji; deterministic from user id |
| `ProfileNameEditor` | `Form` + `Input` + `Button` (shadcn) | Edit display name in Perfil tab |
| `Toast` / `Toaster` | `Sonner` (shadcn) | Success / warning / error toasts |
| `EmptyState` | custom | Reusable: icon + heading + body copy + optional CTA |
| `LoadingSkeleton` | `Skeleton` (shadcn) | For async data regions |

---

## Copywriting Contract

**Language:** Spanish neutro/LATAM. All strings in `es.ts` dictionary. Zero English visible in UI.

### Auth Flow

| Element | Copy |
|---------|------|
| Welcome screen heading | `Bienvenido a SharedTrip` |
| Welcome screen subheading | `Tu bóveda de viaje — boletos, itinerario y más, siempre a la mano.` |
| Magic link input placeholder | `tu@email.com` |
| Magic link input label | `Correo electrónico` |
| Primary CTA — request magic link | `Enviar enlace de acceso` |
| Post-submit heading | `Revisa tu correo` |
| Post-submit body | `Te enviamos un enlace a {email}. Toca el enlace para entrar — expira en 15 minutos.` |
| Email subject template | `Acceso a tu viaje · {HH:mm}` |
| Email body greeting | `Hola 👋  Toca el botón para entrar a tu viaje en SharedTrip.` |
| Email CTA button | `Entrar a SharedTrip` |
| Email footer | `El enlace expira en 15 minutos. Si no lo solicitaste, ignora este correo.` |

### Anonymous Join + Upgrade

| Element | Copy |
|---------|------|
| "Sin cuenta" pill label | `Sin cuenta` |
| Upgrade banner heading | `Sin email guardado — agrega uno para no perder acceso.` |
| Upgrade banner CTA | `Agregar email` |
| Upgrade sheet heading | `Guarda tu acceso` |
| Upgrade sheet body | `Agrega tu email para no perder acceso si cierras la app.` |
| Upgrade email input label | `Tu correo electrónico` |
| Upgrade submit CTA | `Guardar email` |
| Upgrade success toast | `Te enviamos un correo de confirmación a {email}. Toca el enlace para terminar.` |

### Trip Shell — Empty States

| Element | Copy |
|---------|------|
| Trip switcher empty heading | `No tienes viajes todavía` |
| Trip switcher empty body | `Pídele un link a quien te invitó o crea uno.` |
| Docs tab empty heading | `Bóveda de documentos` |
| Docs tab empty body | `Aquí irán los boletos, reservaciones e identificaciones del grupo. Disponible en la siguiente fase.` |
| Itin tab empty heading | `Itinerario del viaje` |
| Itin tab empty body | `El itinerario compartido llega pronto.` |
| Gente tab empty heading | `Miembros del viaje` |
| Gente tab empty body | `La lista del grupo aparecerá aquí. Disponible en la siguiente fase.` |
| Perfil tab display name label | `Tu nombre en el viaje` |
| Perfil tab display name placeholder | `Nombre visible para el grupo` |
| Perfil tab save CTA | `Guardar cambios` |

### Error States

| Element | Copy |
|---------|------|
| Magic link invalid / expired | `Este enlace ya expiró o no es válido. Solicita uno nuevo.` |
| Magic link request error | `No pudimos enviarte el enlace. Verifica tu correo e intenta de nuevo.` |
| Join invalid token | `Este link de invitación no es válido. Pide uno nuevo a quien te invitó.` |
| Session expired | `Tu sesión expiró. Ingresa de nuevo.` |
| Generic network error | `Error de conexión. Verifica tu red e intenta de nuevo.` |

### Destructive Actions

| Action | Trigger | Confirmation approach |
|--------|---------|----------------------|
| Sign out | "Cerrar sesión" button in Perfil tab | `AlertDialog` (shadcn): heading `¿Cerrar sesión?`, body `Tendrás que volver a ingresar con tu correo.`, confirm button `Sí, cerrar sesión` (destructive/red), cancel `Cancelar` |

---

## Interaction Contracts

### Bottom Tab Bar
- Active tab: coral underline 2px + coral icon tint + coral label text
- Inactive tab: `--color-fg-muted` icon + label
- Tap: immediate visual response (no delay); tab content swaps without page navigation animation
- Height: 56px fixed; positioned `fixed bottom-0` with `padding-bottom: env(safe-area-inset-bottom)` for iPhone home indicator
- Tab order (left to right): Docs / Itin / Gente / Perfil
- Tab labels: 14px Inter weight 400 (Label token); icons: 24px Lucide

### Top Header
- Height: 56px; `sticky top-0`; background `--color-surface`; bottom border 1px `--color-fg-muted` at 20% opacity
- Left: trip name as tappable dropdown trigger (chevron icon right of text); text truncated at 160px max-width
- Right: user avatar (32px circle) — if anonymous, "Sin cuenta" pill (mango) appears left of avatar
- No back button in Phase 1 (single-level navigation within trip shell)

### Magic Link Form
- Email input: `type="email"`, autocomplete `email`, `inputmode="email"` for mobile keyboard
- Submit button: full-width, coral, disabled while loading; shows spinner (Lucide `Loader2` with `animate-spin`)
- Validation: Zod `z.string().email()` — inline error below input in `--color-destructive` at 14px weight 400
- Error display: shown on blur AND on submit attempt; not on keystroke
- After successful submit: form replaced by confirmation message (no navigation)

### "Sin cuenta" Pill
- Display condition: `session.user.is_anonymous === true` AND user is on any screen with the top header
- Hidden: on auth/welcome screen, on `/auth/check-email`
- Tap: opens `AnonymousUpgradeSheet`

### Upgrade Nudge Banner
- Display condition: same as "Sin cuenta" pill (anonymous session)
- Position: immediately below `TopHeader`, above tab content
- Dismiss: X button (session-scoped — reappears next launch)
- Animation: slides down from header on mount (CSS transition, 200ms ease-out)
- Hidden: on auth/welcome screen

### Toast Notifications
- Position: top-center on mobile (Sonner default)
- Duration: success = 4s; error = 6s; warning = 5s
- No stacking more than 2 toasts simultaneously

### Loading States
- Data-fetching regions: `Skeleton` component, matching the shape of the content that will appear
- Button loading: replace label with `Loader2` spinner (Lucide, `animate-spin`) — do not disable without showing spinner
- Full-screen loading (route transitions): none in Phase 1 (placeholder tabs load instantly)

### AlertDialog (sign-out)
- Destructive confirm button uses `--color-destructive` background
- Cancel is always the left / less prominent button
- Dialog triggered only once (no "Are you sure you're sure")

---

## PWA / Manifest Contract

The `manifest.json` ships in Phase 1. Service worker (Serwist) ships in Phase 3.

| Property | Value |
|----------|-------|
| `name` | `SharedTrip` |
| `short_name` | `SharedTrip` |
| `lang` | `es` |
| `display` | `standalone` |
| `theme_color` | `#0F1729` (matches `--color-bg`) |
| `background_color` | `#0F1729` |
| PWA icon | Coral square `#FF6B6B` with white `S` centered; sizes 192×192 and 512×512 |
| `start_url` | `/` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Input, Form, Sheet, AlertDialog, Toast/Sonner, Skeleton | not required |
| third-party | none | not applicable |

No third-party shadcn registries are declared for Phase 1.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Source Traceability

| Decision | Source |
|----------|--------|
| Palette (all 7 tokens) | CONTEXT.md D-05 |
| Bottom tab bar (4 tabs, coral active) | CONTEXT.md D-06 |
| Inter variable font, self-hosted | CONTEXT.md D-07 |
| Wordmark-only brand mark, coral italic 700 | CONTEXT.md D-08 |
| Trip switcher as header dropdown (no /viajes route) | CONTEXT.md D-09 |
| Anonymous join → direct trip render, no welcome screen | CONTEXT.md D-10 |
| "Sin cuenta" pill (mango) | CONTEXT.md D-11 |
| Persistent dismissible upgrade banner | CONTEXT.md D-12 |
| Email-only upgrade form, confirmation toast copy | CONTEXT.md D-13 |
| Auto-generated `{Adjective} {Animal}` display name | CONTEXT.md D-14 |
| Emoji + colored circle avatar | CONTEXT.md D-15 |
| User edits own name only (Perfil tab) | CONTEXT.md D-16 |
| `profiles` table, RLS any-member-SELECT / self-UPDATE | CONTEXT.md D-17 |
| Dark-mode only (no toggle in Phase 1) | CONTEXT.md D-05 note |
| Resend sender identity format | CONTEXT.md D-02 |
| Email subject template with timestamp | CONTEXT.md D-03 |
| Email body skeleton and CTA copy | CONTEXT.md D-04 |
| Magic link expiry: 15 minutes | CONTEXT.md D-04 footer |
| shadcn/ui + Tailwind v4 CSS-first | CLAUDE.md / CONTEXT.md |
| All strings in `es.ts`, no hardcoded Spanish in JSX | CONTEXT.md code patterns, REQUIREMENTS.md INFRA-07 |
| Typography: 2 weights only (400/700) | UI checker revision 2026-05-29 |
| Typography: 4 sizes only, 12px exception removed | UI checker revision 2026-05-29 |
| Welcome screen focal point declaration | UI checker revision 2026-05-29 |

---

*Phase: 01-Foundation+Auth*
*UI-SPEC created: 2026-05-29*
*UI-SPEC revised: 2026-05-29 — fixed typography weight/size violations per checker*
*Status: draft — awaiting checker validation*

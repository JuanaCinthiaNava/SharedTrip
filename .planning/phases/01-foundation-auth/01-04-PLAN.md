---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/i18n/es.ts
  - src/app/manifest.ts
  - public/icon-192.png
  - public/icon-512.png
  - src/lib/utils/avatar.ts
  - src/lib/utils/avatar.test.ts
  - src/components/profile/UserAvatar.tsx
  - src/components/profile/ProfileNameEditor.tsx
  - src/components/layout/BottomTabBar.tsx
  - src/components/layout/TopHeader.tsx
  - src/components/layout/TripSwitcherSheet.tsx
  - src/components/common/EmptyState.tsx
  - src/app/t/[tripId]/layout.tsx
  - src/app/t/[tripId]/docs/page.tsx
  - src/app/t/[tripId]/itin/page.tsx
  - src/app/t/[tripId]/gente/page.tsx
  - src/app/t/[tripId]/perfil/page.tsx
  - src/actions/profile.ts
# files_modified count (18) intentionally exceeds the 5-8 target — justified per MEDIUM-8 (2026-05-29):
# the trip shell (TopHeader + BottomTabBar + TripSwitcherSheet + EmptyState + 3 empty tab pages +
# trip layout w/ auth guard) + Perfil (page + ProfileNameEditor + updateProfile action) + PWA
# manifest + 2 icons are a single vertical slice. Splitting into a 6th plan with one wave each
# would add a wave for marginal benefit; the checkpoint at end of plan validates the whole slice.
autonomous: false
requirements:
  - INFRA-07
  - AUTH-04
  - UI-01
  - UI-02
  - UI-03

user_setup: []

must_haves:
  truths:
    - "An authenticated user navigating to `/t/{seed_trip_id}/docs` sees the bottom-tab shell with 4 tabs (Docs/Itin/Gente/Perfil), the active tab (Docs) underlined coral, and a Spanish empty state heading 'Bóveda de documentos'"
    - "Tapping each tab navigates between the four routes without a full page load (Next.js App Router segment swap)"
    - "Tapping the trip name in the top header opens a bottom sheet listing user's trips + a '+ Crear nuevo viaje' empty entry (non-functional in Phase 1)"
    - "Tapping the avatar in the top header navigates to `/t/{tripId}/perfil` where the user sees their auto-generated display name and can edit it"
    - "Submitting a new display name calls `updateProfile` server action → `UPDATE profiles SET display_name=... WHERE id=auth.uid()` → re-renders the new name in the header avatar after revalidation"
    - "Tapping 'Cerrar sesión' opens an AlertDialog; confirming clears the session and returns the user to `/`"
    - "The PWA manifest is served at `/manifest.webmanifest`, validates per W3C, and includes coral icons 192×192 and 512×512 with `lang: es`"
    - "An unauthenticated user navigating to any `/t/[tripId]/...` route is redirected to `/` (server-side guard in the layout)"
  artifacts:
    - path: "src/app/manifest.ts"
      provides: "Next.js MetadataRoute.Manifest with Spanish lang, standalone display, coral theme color, both icon sizes"
      contains: "lang: 'es'"
    - path: "src/app/t/[tripId]/layout.tsx"
      provides: "Trip shell layout — TopHeader + child route + BottomTabBar. Guards against unauthenticated users."
      contains: "BottomTabBar"
    - path: "src/components/layout/BottomTabBar.tsx"
      provides: "Fixed 56px bottom tab bar with 4 Lucide-icon tabs + safe-area inset"
      contains: "safe-area-inset-bottom"
    - path: "src/components/layout/TopHeader.tsx"
      provides: "56px sticky header — trip name dropdown trigger + avatar"
      contains: "TripSwitcherSheet"
    - path: "src/components/profile/UserAvatar.tsx"
      provides: "32px colored circle + animal emoji computed via getAvatarData(userId)"
      contains: "getAvatarData"
    - path: "src/lib/utils/avatar.ts"
      provides: "getAvatarData(userId, avatarSeed?) deterministic name + emoji + color"
      exports: ["getAvatarData"]
    - path: "src/components/profile/ProfileNameEditor.tsx"
      provides: "RHF form for updating display_name"
      contains: "updateProfile"
    - path: "src/actions/profile.ts"
      provides: "updateProfile(displayName: string) server action"
      exports: ["updateProfile"]
    - path: "src/components/common/EmptyState.tsx"
      provides: "Reusable empty-state primitive — icon + heading + body + optional CTA"
      contains: "EmptyState"
    - path: "public/icon-192.png"
      provides: "PWA icon 192x192 (coral background, white S)"
    - path: "public/icon-512.png"
      provides: "PWA icon 512x512 (coral background, white S, also used as maskable)"
  key_links:
    - from: "src/app/t/[tripId]/layout.tsx"
      to: "src/lib/supabase/server.ts (getUser)"
      via: "auth guard — redirect to / if no session"
      pattern: "getUser"
    - from: "src/components/profile/UserAvatar.tsx"
      to: "src/lib/utils/avatar.ts"
      via: "getAvatarData(user.id, profile.avatar_seed)"
      pattern: "getAvatarData"
    - from: "src/actions/profile.ts"
      to: "supabase.from('profiles').update"
      via: ".update({ display_name }).eq('id', user.id)"
      pattern: "from\\(['\"]profiles['\"]\\)\\.update"
    - from: "src/app/manifest.ts"
      to: "public/icon-192.png + public/icon-512.png"
      via: "icons array referencing /icon-192.png, /icon-512.png"
      pattern: "icon-(192|512)\\.png"
---

<objective>
Ship the trip shell + Perfil + PWA manifest. After this plan, an authenticated user (signed in via Plan 03) can navigate to `/t/{seed-trip-id}/docs`, see the bottom tab bar with empty-state Spanish copy on all four tabs, switch to Perfil, edit their display name, and sign out via an AlertDialog confirmation. The PWA manifest also goes live, making the app installable to iOS Home Screen (service worker still deferred to Phase 3).

Purpose: Establish the shell pattern every Phase 2-4 tab fills in. The component layout, the avatar generator, the empty-state primitive, and the auth-guard pattern in the trip layout are all conventions later phases inherit unchanged.

Output: A fully navigable Spanish app shell with all 4 tabs rendering empty states, plus a functional Perfil tab (edit display name + sign out), plus a PWA manifest. After this plan, Plan 05 wires the anonymous-join path on top of this shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-SKELETON.md
@.planning/phases/01-foundation-auth/01-01-PLAN.md
@.planning/phases/01-foundation-auth/01-02-PLAN.md
@.planning/phases/01-foundation-auth/01-03-PLAN.md

<interfaces>
Contracts for Plan 05 to consume:

`src/lib/utils/avatar.ts` exports `getAvatarData(userId, avatarSeed?) -> { displayName, emoji, color }`. The full implementation (30 adjectives + 30 animals + emoji map + djb2 hash + color picker) is in RESEARCH Pattern 10 — copy verbatim. Plan 05's AnonymousBanner reuses UserAvatar which calls getAvatarData.

`src/components/profile/UserAvatar.tsx` exports `<UserAvatar userId avatarSeed displayName size?>`. Sizes: sm=24px, md=32px, lg=64px.

`src/components/layout/TopHeader.tsx` exports `<TopHeader tripId tripName userId avatarSeed displayName isAnonymous? />`. Plan 04 ships the header with `isAnonymous` prop accepted but renders no pill yet. Plan 05 adds the pill when `isAnonymous=true`.

`src/components/common/EmptyState.tsx` exports `<EmptyState heading body icon? cta?>`. Used by all four placeholder tabs.

`src/actions/profile.ts` exports `updateProfile({ displayName })`. Plan 05 adds `upgradeToEmail(email)` calling `supabase.auth.updateUser({ email })`.

`src/i18n/es.ts` AFTER this plan must extend with `es.tabs.*`, `es.profile.*`, `es.tripSwitcher.*` from RESEARCH Pattern 9. Plan 05 will add `es.anon.*` later.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Avatar generator + UserAvatar + EmptyState + es.ts extensions</name>
  <files>src/lib/utils/avatar.ts, src/lib/utils/avatar.test.ts, src/components/profile/UserAvatar.tsx, src/components/common/EmptyState.tsx, src/i18n/es.ts</files>
  <read_first>
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (Pattern 10 Deterministic Avatar Name Generator — copy verbatim; Pattern 9 for es.ts extensions)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md (D-14 auto-name, D-15 emoji+color, D-17 profiles shape)
    - .planning/phases/01-foundation-auth/01-UI-SPEC.md (UserAvatar component + Empty State + Component Inventory)
    - src/types/database.ts (Profiles row type)
  </read_first>
  <behavior>
    getAvatarData(userId, avatarSeed) behavior:
    - Given a fixed userId UUID, output is deterministic (same input -> same output every call).
    - When avatarSeed is provided, hash is computed over the seed, NOT the userId.
    - Output color is one of exactly three values: #FF6B6B, #3DCCC7, #FFB627.
    - Output displayName matches pattern Adjective+space+Animal with Spanish accents allowed.
    - Output emoji is a single grapheme cluster from the ANIMAL_EMOJIS map.
    - For 1000 random UUIDs, distribution across the three colors is roughly 33% each (accept plus or minus 5%).

    EmptyState behavior:
    - Renders heading and body as separate text nodes.
    - When icon prop provided, renders the Lucide icon above the heading.
    - When cta provided, renders a Button + Link with cta label.
    - When cta undefined, no button renders.
  </behavior>
  <action>
    Create src/lib/utils/avatar.ts — copy verbatim from RESEARCH Pattern 10. Place a header comment: Sourced from .planning/phases/01-foundation-auth/01-RESEARCH.md Pattern 10. Include all 30 ADJECTIVES, 30 ANIMALS, ANIMAL_EMOJIS map, AVATAR_COLORS triple, hashUserId djb2 function, and getAvatarData export.

    Create src/lib/utils/avatar.test.ts using vitest (install vitest as devDep + add npm script "test": "vitest run" if not present from create-next-app). Tests must verify the four behaviors above. Use ten fixed UUIDs as snapshot inputs to catch accidental hash changes.

    Create src/components/profile/UserAvatar.tsx:
    - 'use client' directive.
    - Props: { userId, avatarSeed?, displayName?, size? }.
    - Compute { emoji, color, displayName: autoName } = getAvatarData(userId, avatarSeed).
    - Render a div with inline style backgroundColor=color (the ONE permitted exception to "no hex in components" — color is from the curated AVATAR_COLORS set, not arbitrary). className: inline-flex items-center justify-center rounded-full.
    - Size classes: sm w-6 h-6 text-base, md w-8 h-8 text-lg, lg w-16 h-16 text-3xl. Emoji renders inside.
    - aria-label set to displayName ?? autoName for accessibility.

    Create src/components/common/EmptyState.tsx:
    - Server-Component-compatible (no 'use client'). CTA accepted as { label, href } (Next Link), not as onClick — keeps server rendering working and avoids client-only requirement.
    - Props: { heading, body, icon?, cta? }.
    - Layout: centered column, vertical padding py-16 (3xl maps to 64px in Tailwind defaults). Icon 48px (w-12 h-12 text-fg-muted). Heading 20/700/1.3. Body 16/400/1.5 text-fg-muted. Optional CTA as shadcn Button wrapped in Next Link.

    Extend src/i18n/es.ts via Edit tool — add the three namespaces es.tabs, es.profile, es.tripSwitcher with the keys from RESEARCH Pattern 9 verbatim. Keep the 'as const' assertion.
  </action>
  <verify>
    <automated>cd shared-trip && npm test -- --run 2>&1 | tail -10 | grep -E '(passed|PASS|ok)' && npm run build 2>&1 | tail -3 && grep -c 'getAvatarData' src/components/profile/UserAvatar.tsx | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 and reports at least 5 passing test cases for avatar.test.ts.
    - npm run build exits 0.
    - grep -c 'ADJECTIVES' src/lib/utils/avatar.ts returns at least 1.
    - src/i18n/es.ts contains top-level keys tabs:, profile:, tripSwitcher: inside the es constant.
    - grep -cE 'docsEmptyHeading|itinEmptyHeading|signOutCta|displayNameLabel' src/i18n/es.ts returns at least 4.
    - src/components/profile/UserAvatar.tsx has an aria-label attribute.
    - src/components/common/EmptyState.tsx does NOT import any Spanish string literally — all copy via props.
  </acceptance_criteria>
  <done>Avatar generator covered by tests (10 fixed UUIDs + distribution); UserAvatar + EmptyState shipped; es.ts extended with the three new namespaces.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BottomTabBar + TopHeader + TripSwitcherSheet + trip-scoped layout (auth guard) + three empty-state tab pages</name>
  <files>src/components/layout/BottomTabBar.tsx, src/components/layout/TopHeader.tsx, src/components/layout/TripSwitcherSheet.tsx, src/app/t/[tripId]/layout.tsx, src/app/t/[tripId]/docs/page.tsx, src/app/t/[tripId]/itin/page.tsx, src/app/t/[tripId]/gente/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-auth/01-UI-SPEC.md (Component Inventory + Interaction Contracts: Bottom Tab Bar + Top Header + Spacing constraints)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md (D-06 bottom tabs, D-09 trip switcher dropdown)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (System Architecture diagram for trip-scoped routes)
    - src/lib/supabase/server.ts (server factory for the auth guard)
    - src/components/profile/UserAvatar.tsx (from Task 1)
    - src/components/common/EmptyState.tsx (from Task 1)
    - src/i18n/es.ts (consumed via es.tabs.*, es.tripSwitcher.*)
    - src/components/ui/sheet.tsx (shadcn primitive from Plan 01)
  </read_first>
  <action>
    Create src/components/layout/BottomTabBar.tsx:
    - 'use client' (uses usePathname from next/navigation).
    - Fixed bottom, 56px height, padding via Tailwind pb-[env(safe-area-inset-bottom)] for iPhone home indicator clearance.
    - Background bg-surface, top border 1px border-border.
    - 4 tabs in this order: Docs / Itin / Gente / Perfil. Lucide icons FileText, Calendar, Users, User at 24px.
    - Each tab is a Next Link href={`/t/${tripId}/${tab}`} styled as a column flex with gap-1, 44px min touch height.
    - Active state (pathname starts with `/t/{tripId}/{tab}`): icon + label text-primary (coral) with a 2px coral border-b-2 indicator inside the touch area.
    - Inactive: icon + label text-fg-muted.
    - Labels via es.tabs.docs, es.tabs.itin, es.tabs.gente, es.tabs.perfil at 14px Inter weight 400.

    Create src/components/layout/TopHeader.tsx:
    - 'use client' (Sheet open state).
    - Props: { tripId, tripName, userId, avatarSeed, displayName, isAnonymous?, trips }. trips array passed in from server layout for TripSwitcherSheet to render.
    - 56px height, sticky top-0, bg-surface, bottom border, z-index above content.
    - Left: TripSwitcherSheet rendered with the trips prop; the trigger inside is a button with text-base font-bold truncate max-w-[160px] showing tripName plus a ChevronDown icon ml-1 w-4 h-4 text-fg-muted.
    - Right: a Next Link href={`/t/${tripId}/perfil`} wrapping UserAvatar size=md with the userId/avatarSeed/displayName props.
    - Do NOT render the "Sin cuenta" pill here — Plan 05 fills that slot. Accept isAnonymous prop but use it ONLY as a comment placeholder for now.

    Create src/components/layout/TripSwitcherSheet.tsx:
    - 'use client'.
    - Composed via shadcn Sheet with side="bottom".
    - Accepts tripId (current) and trips: Array<{id, name}> prop. Renders one row per trip (Link to /t/{id}/docs) + a non-functional row at the bottom: <div>+ Crear nuevo viaje</div> (Phase 2 wires creation). The non-functional row may be a disabled button with the literal label sourced from es (add es.tripSwitcher.createCta="+ Crear nuevo viaje" if not present, otherwise inline literal is acceptable ONLY here as it duplicates the trip-switcher empty heading — prefer adding the key to es.ts for INFRA-07 compliance).
    - If trips.length === 0: render EmptyState with heading=es.tripSwitcher.emptyHeading, body=es.tripSwitcher.emptyBody.

    Create src/app/t/[tripId]/layout.tsx (Server Component — auth guard + data fetch):
    - Async server component. Read { tripId } from await params.
    - const supabase = await createClient() (server factory). const { data: { user } } = await supabase.auth.getUser(). If !user, redirect('/') from next/navigation (AUTH guard).
    - Fetch trip: const { data: trip } = await supabase.from('trips').select('id, name').eq('id', tripId).single(). If !trip (RLS denied or not member), redirect('/') — RLS handles the membership check.
    - Fetch user's other trips for switcher: const { data: userTrips } = await supabase.from('trips').select('id, name').neq('id', tripId).
    - Fetch own profile: const { data: profile } = await supabase.from('profiles').select('display_name, avatar_seed').eq('id', user.id).single().
    - Render:
      <TopHeader tripId={tripId} tripName={trip.name} userId={user.id} avatarSeed={profile?.avatar_seed ?? null} displayName={profile?.display_name ?? null} isAnonymous={user.is_anonymous ?? false} trips={userTrips ?? []} />
      <main className="min-h-dvh pt-4 pb-16">{children}</main>
      <BottomTabBar tripId={tripId} />
    - pb-16 (~64px) clears the 56px tab bar (UI-SPEC: "3xl Bottom clearance above tab bar").

    Create the three empty-state pages (docs/page.tsx, itin/page.tsx, gente/page.tsx):
    - Server Components.
    - Each renders <EmptyState icon={FileText|Calendar|Users} heading={es.tabs.{docs|itin|gente}EmptyHeading} body={es.tabs.{docs|itin|gente}EmptyBody} />.
    - No CTAs (Phase 2/3/4 fill these).

    DO NOT use hardcoded Spanish anywhere. DO NOT hardcode hex colors (UserAvatar bg-color exception is the only one). DO NOT add a functional trip-creation handler (deferred to Phase 2 per D-09).
  </action>
  <verify>
    <automated>cd shared-trip && npm run build 2>&1 | tail -3 && grep -c 'safe-area-inset-bottom\|pb-\[env' src/components/layout/BottomTabBar.tsx | grep -v '^0$' && grep -c 'getUser' "src/app/t/[tripId]/layout.tsx" | grep -v '^0$' && grep -c 'redirect' "src/app/t/[tripId]/layout.tsx" | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0.
    - src/app/t/[tripId]/layout.tsx contains both getUser AND redirect (auth guard present).
    - src/components/layout/BottomTabBar.tsx has the safe-area-inset-bottom env reference.
    - Three routes `src/app/t/[tripId]/{docs,itin,gente}/page.tsx` all exist after this task (perfil is DEFERRED to Task 3 — at end of Task 2, `test ! -f src/app/t/[tripId]/perfil/page.tsx` is acceptable; perfil existence is checked separately in Task 3's acceptance_criteria).
    - grep -v '^//' src/components/layout/BottomTabBar.tsx src/components/layout/TopHeader.tsx | grep -cE 'Docs|Itin|Gente|Perfil|Bóveda|itinerario|Miembros' returns 0 (zero hardcoded Spanish — all via es.tabs.*).
    - grep -cE 'usePathname' src/components/layout/BottomTabBar.tsx returns at least 1 (active-tab detection).
    - grep -cE 'Lucide|lucide-react' src/components/layout/BottomTabBar.tsx returns at least 1.
  </acceptance_criteria>
  <done>Bottom tab bar + top header + trip switcher sheet built. Trip-scoped layout guards against unauthenticated users via server-side redirect. Three empty-state tab pages rendering Spanish copy from es.tabs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Perfil tab — display name editor + sign-out AlertDialog + updateProfile server action + PWA manifest + icons</name>
  <files>src/app/t/[tripId]/perfil/page.tsx, src/components/profile/ProfileNameEditor.tsx, src/actions/profile.ts, src/app/manifest.ts, public/icon-192.png, public/icon-512.png</files>
  <read_first>
    - .planning/phases/01-foundation-auth/01-UI-SPEC.md (Component Inventory: ProfileNameEditor + Destructive Actions table)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md (D-16 only user edits own name, D-17 profiles shape)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (Pattern 7: PWA Manifest — copy verbatim)
    - src/actions/auth.ts (the signOut server action from Plan 03)
    - src/lib/supabase/server.ts (server factory)
    - src/lib/utils/avatar.ts (getAvatarData for fallback display name)
    - src/i18n/es.ts (es.profile.*)
    - src/components/ui/alert-dialog.tsx (shadcn)
  </read_first>
  <action>
    Create src/actions/profile.ts:
    - 'use server' at top.
    - export async function updateProfile(input: { displayName: string }): Promise<{ error: string | null }>. Validates displayName via Zod (z.string().min(1).max(60)). Calls const supabase = await createClient() (server factory). const { data: { user } } = await supabase.auth.getUser(). If !user, return { error: 'Sesión expirada' } (but use es.errors.sessionExpired — Server Actions can import es). Otherwise supabase.from('profiles').update({ display_name: input.displayName }).eq('id', user.id). RLS already restricts UPDATE to id = (SELECT auth.uid()), but the .eq('id', user.id) is defense-in-depth.
    - After UPDATE, call revalidatePath(`/t/[tripId]`, 'layout') from next/cache so TopHeader re-renders with the new name.
    - Return { error: null } on success.

    Create src/components/profile/ProfileNameEditor.tsx:
    - 'use client'.
    - Props: { initialDisplayName: string; userId: string; avatarSeed: string | null }.
    - RHF + Zod schema z.object({ displayName: z.string().min(1, es.errors.sendLinkFailed).max(60) }) — use existing error string or add es.profile.invalidName if cleaner.
    - Default value initialDisplayName ?? getAvatarData(userId, avatarSeed).displayName (so the field is pre-filled with the auto-generated name).
    - shadcn Form + Input + Button. Submit button uses es.profile.saveCta.
    - On submit, useTransition + call updateProfile({ displayName }). On error, toast.error. On success, toast.success with a brief "Guardado" message (add es.profile.savedToast if necessary).

    Create src/app/t/[tripId]/perfil/page.tsx (Server Component):
    - Read { tripId } from params.
    - Server factory + getUser + redirect('/') if no session.
    - Fetch own profile: const { data: profile } = await supabase.from('profiles').select('display_name, avatar_seed').eq('id', user.id).single().
    - Render:
      - UserAvatar size="lg" userId={user.id} avatarSeed={profile?.avatar_seed ?? null} displayName={profile?.display_name ?? null} — large 64px avatar at the top.
      - ProfileNameEditor with initialDisplayName, userId, avatarSeed.
      - SignOutSection (separate component, see below).

    Create the sign-out UI inside the perfil page (inline or as a sub-component):
    - 'use client' sub-component (or inline) using shadcn AlertDialog.
    - Trigger: a destructive-variant Button with label es.profile.signOutCta.
    - AlertDialog: heading es.profile.signOutDialogHeading, body es.profile.signOutDialogBody, confirm button es.profile.signOutDialogConfirm with destructive variant (bg-destructive), cancel button es.profile.signOutDialogCancel.
    - On confirm, call signOut() server action from src/actions/auth.ts (already implemented in Plan 03).

    Create src/app/manifest.ts — copy VERBATIM from RESEARCH Pattern 7:
    - export default function manifest(): MetadataRoute.Manifest returning the object with name 'SharedTrip', short_name 'SharedTrip', description in Spanish, start_url '/', display 'standalone', background_color '#0F1729', theme_color '#0F1729', lang 'es', and the icons array with both PNG sizes (192x192 and 512x512 with maskable purpose on the 512 entry).

    Generate the two PNG icons in public/:
    - 192x192 and 512x512.
    - Coral background #FF6B6B, centered white 'S' in Inter Bold.
    - Use any deterministic generation approach the executor prefers — RECOMMENDED: a Node script that uses the sharp library (or `npx sharp-cli`) to render a solid-color SVG with a centered text element and rasterize. Acceptable simpler approach: commit a pre-made PNG file from any image editor that meets the spec. Either way: file presence in public/ is what counts. If using sharp: `npm install -D sharp` then a script under scripts/generate-icons.mjs run once via `node scripts/generate-icons.mjs`.

    DO NOT use Spanish literals in ProfileNameEditor or the sign-out UI — all via es.profile.*. DO NOT use a third-party icon generator service — generate locally so the build is reproducible.
  </action>
  <verify>
    <automated>cd shared-trip && npm run build 2>&1 | tail -3 && test -f public/icon-192.png && test -f public/icon-512.png && test -f src/app/manifest.ts && grep -c "lang: 'es'\|lang: \"es\"" src/app/manifest.ts | grep -v '^0$' && grep -c "updateProfile\|signOut" src/app/t/\[tripId\]/perfil/page.tsx src/components/profile/ProfileNameEditor.tsx | grep -v '^0$' && grep -c 'AlertDialog' src/app/t/\[tripId\]/perfil/page.tsx | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0.
    - public/icon-192.png and public/icon-512.png both exist with file sizes greater than 1KB (file exists with real bitmap content, not empty).
    - src/app/manifest.ts contains lang 'es' AND theme_color '#0F1729' AND both /icon-192.png AND /icon-512.png references.
    - After build, requesting /manifest.webmanifest from the dev server returns valid JSON with all required PWA fields (verified in the checkpoint).
    - src/components/profile/ProfileNameEditor.tsx calls updateProfile from @/actions/profile.
    - `test -f "src/app/t/[tripId]/perfil/page.tsx"` succeeds (perfil route now exists — deferred from Task 2 per HIGH-5 fix 2026-05-29).
    - src/app/t/[tripId]/perfil/page.tsx uses AlertDialog from @/components/ui/alert-dialog.
    - grep -cE 'Cerrar sesión|Tu nombre en el viaje|Guardar cambios' src/components/profile/ProfileNameEditor.tsx src/app/t/\[tripId\]/perfil/page.tsx returns 0 (zero hardcoded Spanish).
    - src/actions/profile.ts contains revalidatePath call (so the header refreshes after rename).
  </acceptance_criteria>
  <done>Perfil tab renders a 64px avatar, the auto-generated name as an editable input, and a sign-out button that triggers an AlertDialog confirmation. PWA manifest live with coral icons. Manifest serves at /manifest.webmanifest.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the trip shell + Perfil + PWA manifest on real iPhone</name>
  <what-built>
    Bottom tab bar with 4 tabs (Docs/Itin/Gente/Perfil) and active-coral indicator.
    Top header with trip name dropdown and avatar.
    TripSwitcherSheet (bottom sheet listing user trips).
    Three empty-state pages (Docs/Itin/Gente).
    Perfil tab with editable display name + sign-out AlertDialog.
    PWA manifest at /manifest.webmanifest with coral icons 192x192 + 512x512.
    Server-side auth guard in /t/[tripId]/layout.tsx (redirects unauthenticated users to /).
  </what-built>
  <how-to-verify>
    Deploy latest commit. Then:

    1. Sign-in side effect: complete the magic link flow from Plan 03 to get a session cookie. Then manually navigate to https://{vercel-domain}/t/11111111-1111-1111-1111-111111111111/docs (the Plan 02 seed trip).

       Expected: 404 OR redirect to / (because the signed-in user is NOT a member of the seed trip — that's Plan 05's job via /join/[token]). To verify the shell actually works in Plan 04, the executor must temporarily INSERT the test user as a member: in Supabase SQL editor, run INSERT INTO public.trip_members (trip_id, user_id, role) VALUES ('11111111-1111-1111-1111-111111111111', '<your-user-id>', 'member'). Then re-navigate.

       After membership insert + navigate, expected: bottom tab bar visible with Docs underlined coral; top header shows "Viaje de Prueba (Phase 1)" with chevron; right side shows colored avatar circle with an animal emoji.

    2. Tab switching: tap each of Itin, Gente, Perfil. Confirm each route loads with the appropriate empty-state heading (Itin: "Itinerario del viaje"; Gente: "Miembros del viaje"; Perfil: avatar + name editor + sign-out button).

    3. Trip switcher: tap the trip name in the header. A bottom sheet slides up showing the empty state "No tienes viajes todavía" (no other trips for this user) plus the "+ Crear nuevo viaje" row (non-functional — confirms Plan 04 ships the UI, Phase 2 wires the action).

    4. Perfil → edit name: in Perfil tab, the input is pre-filled with an auto-generated Spanish name like "Tucán Curioso" or similar (one of 30 adjectives + 30 animals). Change to a custom name, tap "Guardar cambios". Toast confirms save. Return to Docs tab. Tap trip name dropdown back, return to Perfil. New name persists (means the UPDATE went to profiles and revalidatePath worked).

    5. Sign-out: in Perfil, tap "Cerrar sesión". AlertDialog appears with heading "¿Cerrar sesión?". Tap "Sí, cerrar sesión". Returned to /. Now attempting to navigate back to /t/{seed-trip-id}/docs should redirect to / (auth guard).

    6. Auth guard: in a fresh Incognito window, navigate to https://{vercel-domain}/t/11111111-1111-1111-1111-111111111111/docs. Expected: instant redirect to /.

    7. PWA manifest: open https://{vercel-domain}/manifest.webmanifest in Safari. Confirm valid JSON with name "SharedTrip", lang "es", display "standalone", icons array with 192 + 512. In Safari on iPhone, tap Share -> "Add to Home Screen". Confirm the icon previewed is the coral square with white S. (Do NOT actually install — Phase 5 is the real install test. Just confirm the icon renders in the preview.)

    8. Build sanity: npm run build locally — exits 0; no TypeScript errors.

    If ANY step fails, describe step number + actual behavior. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Server Action (updateProfile) | Untrusted displayName input; Zod validates max-length 60. |
| Browser -> trip layout | Server fetches user/profile/trips; RLS enforces that only members can see trip data. |
| Public icon files | Static binary served from CDN; no user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | A user updates someone else's profile by passing a different id to updateProfile | mitigate | updateProfile ignores any id input; uses (await supabase.auth.getUser()).id only. Combined with profiles RLS UPDATE policy id = (SELECT auth.uid()) — defense-in-depth. |
| T-04-02 | Information Disclosure | A user navigates to /t/{otherUserTrip}/docs and sees the trip | mitigate | trip layout fetches via .eq('id', tripId).single() with RLS; if user is not a member, the row is filtered out and the layout calls redirect('/'). The browser never sees the trip name. |
| T-04-03 | Cross-Site Scripting | display_name rendered without escaping → user injects script | mitigate | React JSX auto-escapes string text content. ProfileNameEditor accepts plain string only. displayName is bound to {} text node, never dangerouslySetInnerHTML. |
| T-04-04 | Tampering | Avatar bg color computed from untrusted user id leads to non-curated hex | accept | userId is a UUID from auth.users — Supabase-generated; cannot be user-influenced. Even if it were, the hash is constrained to index into the AVATAR_COLORS triple. Output color is always one of three values. |
| T-04-05 | Denial of Service | User submits a 1 MB display_name | mitigate | Zod .max(60) on the Server Action input. Postgres profiles.display_name is type text (no DB-level cap) — the Zod cap is the enforcement. |
| T-04-06 | Spoofing | Malicious /manifest.webmanifest served by attacker | accept | Manifest is served from same origin as the app; same-origin policy applies. Phishing risk reduces to T-01-04. |
| T-04-SC | Tampering | New dependency `sharp` (if used for icon generation) | mitigate | sharp is the de-facto Node image-processing library (lovell/sharp, 50M+ weekly downloads). Status: [ASSUMED] per Package Legitimacy Audit. If executor avoids sharp by committing static PNGs from an image editor, no new dependency is added — preferred for legitimacy minimization. |
</threat_model>

<verification>
- npm run build exits 0 with no TypeScript errors.
- /manifest.webmanifest returns valid JSON with all required PWA fields.
- Unauthenticated request to /t/[anything]/docs redirects to / (auth guard works).
- Authenticated member request to /t/{seed-trip}/docs renders the tab shell with the coral active indicator.
- Updating display name persists via Server Action + RLS + revalidatePath; new name appears in TopHeader after navigation.
- Sign-out AlertDialog confirms, signOut Server Action clears the cookie session, redirects to /.
- All Spanish strings in shell components source from es.tabs/profile/tripSwitcher; zero hardcoded Spanish in components.
</verification>

<success_criteria>
- INFRA-07: All shell strings sourced from es.ts; pattern extended cleanly from Plan 01's foundation.
- AUTH-04: Sign-out from Perfil tab clears the session; auth guard re-engages on next navigation.
- UI-01: Zero English visible across shell + Perfil + empty states.
- UI-02: Tropical Sunset palette consistently applied via @theme tokens.
- UI-03: Shell + Perfil functional from 320px to desktop without horizontal scroll.
- PWA manifest live with installable coral icons.
</success_criteria>

<output>
Create .planning/phases/01-foundation-auth/01-04-SUMMARY.md. Include: the final shape of es.ts (so Plan 05 can extend with es.anon.*), the props signature of TopHeader (so Plan 05 knows the isAnonymous slot to fill), and confirmation that the auth guard + PWA manifest are live.
</output>

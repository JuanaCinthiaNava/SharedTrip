---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260530000001_initial_schema.sql
  - supabase/migrations/20260530000002_storage_bucket_rls.sql
  - supabase/migrations/20260530000003_profile_autocreate_trigger.sql
  - supabase/migrations/20260530000004_phase1_seed_test_trip.sql
  - src/types/database.ts
  - .github/workflows/keep-alive.yml
  - .gitignore
autonomous: false
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05

user_setup:
  - service: supabase
    why: "Schema push requires either Supabase CLI login or SUPABASE_ACCESS_TOKEN env var; storage bucket creation requires dashboard click (CLI cannot create buckets remotely without API)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens → Generate new token (PAT). Use for non-TTY `supabase db push`."
    dashboard_config:
      - task: "Create private Storage bucket named 'trip-documents'"
        location: "Supabase Dashboard → Storage → New bucket → name 'trip-documents', Public OFF, file size limit 10 MB"
      - task: "Set OTP / Magic Link expiry to 900 seconds (15 minutes)"
        location: "Supabase Dashboard → Authentication → Email Templates → Email OTP Expiration"
      - task: "Enable Anonymous Sign-ins"
        location: "Supabase Dashboard → Authentication → Providers → Anonymous → Enable"
  - service: github
    why: "Keep-alive cron requires GitHub Actions secrets (publishable key + Supabase URL)"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Add as GitHub Actions repo secret (Settings → Secrets and variables → Actions → New repository secret)"
      - name: SUPABASE_PUBLISHABLE_KEY
        source: "Add as GitHub Actions repo secret (same value as NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY)"

must_haves:
  truths:
    - "An unauthenticated user querying any of `trips`, `trip_members`, `documents`, `itinerary_items`, `expenses`, `profiles` from the client SDK gets zero rows (RLS is on, no policies match)"
    - "An authenticated user (anon or permanent) querying `trips` sees only trips for which `is_trip_member(id)` returns true"
    - "A row exists in `public.trips` with the seed UUID and seed `invite_token` (Plan 04 will join against this token to validate the anonymous-join flow)"
    - "The Storage bucket `trip-documents` exists, is private, and has RLS policies allowing SELECT/INSERT only to `is_trip_member((storage.foldername(name))[1]::uuid)`"
    - "GitHub Actions workflow `keep-alive.yml` runs every 5 minutes via cron and hits the Supabase REST `/rest/v1/` endpoint successfully (verified by a manual `workflow_dispatch` run returning HTTP 200)"
    - "TypeScript types for the database (`src/types/database.ts`) are generated and match the schema — `import { Database } from '@/types/database'` resolves; `Database['public']['Tables']['trips']['Row']` has fields `id`, `name`, `invite_token`, etc."
  artifacts:
    - path: "supabase/migrations/20260530000001_initial_schema.sql"
      provides: "All 6 tables (profiles, trips, trip_members, documents, itinerary_items, expenses) + is_trip_member function + RLS policies in ONE file"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "supabase/migrations/20260530000002_storage_bucket_rls.sql"
      provides: "Storage RLS policies on storage.objects for the trip-documents bucket"
      contains: "storage.foldername"
    - path: "supabase/migrations/20260530000003_profile_autocreate_trigger.sql"
      provides: "Trigger on auth.users INSERT that creates a public.profiles row with NULL display_name (client fills via Plan 05 ProfileNameEditor)"
      contains: "ON auth.users"
    - path: "supabase/migrations/20260530000004_phase1_seed_test_trip.sql"
      provides: "Seed row in trips so Plan 04 can validate the /join/[token] flow against a real invite_token"
      contains: "INSERT INTO public.trips"
    - path: "src/types/database.ts"
      provides: "Generated TS types matching the schema; consumed by Supabase client factories in Plan 03"
      contains: "Database"
    - path: ".github/workflows/keep-alive.yml"
      provides: "Cron every 5 min pinging Supabase REST"
      contains: "*/5 * * * *"
  key_links:
    - from: "supabase/migrations/20260530000001_initial_schema.sql"
      to: "auth.users (Supabase built-in)"
      via: "REFERENCES auth.users ON DELETE CASCADE"
      pattern: "REFERENCES auth\\.users"
    - from: "supabase/migrations/20260530000002_storage_bucket_rls.sql"
      to: "is_trip_member function defined in migration 01"
      via: "USING (is_trip_member(...)::boolean)"
      pattern: "is_trip_member"
    - from: ".github/workflows/keep-alive.yml"
      to: "Supabase REST /rest/v1/"
      via: "curl GET with apikey header"
      pattern: "/rest/v1/"
---

<objective>
Run the database schema migration (all 6 tables + `is_trip_member()` + every RLS policy + storage RLS in a single coherent set of migrations), generate the TypeScript types from the live schema, configure the storage bucket via the Supabase dashboard, install the GitHub Actions keep-alive cron, and SEED a single test trip so Plan 04 can validate the `/join/[token]` flow end-to-end.

Purpose: Establish the data plane Plans 03/04/05 will write against. The single most important constraint here is **RLS-in-creation-migration** (Pitfall #2): RLS must be enabled in the same migration as the table, never later. The migration file is the source of truth — schema-via-dashboard is forbidden.

Output: A Supabase database that returns zero rows to unauthenticated callers and only allows members to see their own trip data, with TypeScript types matching the schema and a 5-minute keep-alive ping firing from GitHub Actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-SKELETON.md
@.planning/phases/01-foundation-auth/01-01-PLAN.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Schema contract the rest of Phase 1 consumes. Frozen here. -->

The full DDL for `public.profiles`, `public.trips`, `public.trip_members`, `public.documents`, `public.itinerary_items`, `public.expenses`, the `is_trip_member()` function, and ALL RLS policies is in `.planning/phases/01-foundation-auth/01-RESEARCH.md` Pattern 5 — copy that SQL verbatim into the migration file (it has been reviewed for ASVS L1 and the (SELECT auth.uid()) optimization). Do not improvise the SQL.

Storage RLS policies (objects in the `trip-documents` bucket) are in RESEARCH Pattern 5 at the bottom of the SQL block (`Members can read trip files`, `Members can upload trip files`, `Authenticated users can delete own uploads`). They depend on the file-path convention `{tripId}/{filename}` (Pitfall 6) — Plan 03/Phase 3 enforce this in upload code.

Profile autocreate trigger (Plan 05 reads profile rows; they must already exist):
```sql
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
BEGIN
  INSERT INTO public.profiles (id) VALUES (NEW.id) ON CONFLICT (id) DO NOTHING;
  RETURN NEW;
END;
$$;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();
```

Seed row for Plan 04's anonymous-join validation:
```sql
-- Phase 1 test seed: a trip + its admin creator. invite_token is well-known so Plan 04 can test /join/{token}.
-- Plan 02 must INSERT a fake creator into auth.users FIRST (via supabase admin API or by using a real test user id),
-- because public.trips.created_by REFERENCES auth.users. The simplest path:
-- (a) execute the seed AFTER the developer has signed up via magic link at least once (Plan 03),
-- OR (b) seed against a sentinel uuid '00000000-0000-0000-0000-000000000001' for a row inserted directly into auth.users via the SQL editor with the service role key.
-- The pragmatic Plan 02 approach: use (b) — a hardcoded service-role INSERT into auth.users + public.trips, gated behind an idempotent check.
INSERT INTO auth.users (id, email, raw_user_meta_data, aud, role)
VALUES (
  '00000000-0000-0000-0000-000000000001',
  'phase1-test@sharedtrip.invalid',
  '{"display_name":"Phase 1 Seed Admin"}'::jsonb,
  'authenticated',
  'authenticated'
) ON CONFLICT (id) DO NOTHING;

INSERT INTO public.trips (id, name, description, start_date, end_date, created_by, invite_token)
VALUES (
  '11111111-1111-1111-1111-111111111111',
  'Viaje de Prueba (Phase 1)',
  'Trip seeded by Plan 02 so Plan 04 can validate the anonymous-join flow.',
  CURRENT_DATE,
  CURRENT_DATE + INTERVAL '7 days',
  '00000000-0000-0000-0000-000000000001',
  '22222222-2222-2222-2222-222222222222'
) ON CONFLICT (id) DO NOTHING;

INSERT INTO public.trip_members (trip_id, user_id, role)
VALUES (
  '11111111-1111-1111-1111-111111111111',
  '00000000-0000-0000-0000-000000000001',
  'admin'
) ON CONFLICT DO NOTHING;
```
Plan 04 will join `/join/22222222-2222-2222-2222-222222222222` to test the flow.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the four SQL migrations + initialize Supabase CLI project locally</name>
  <files>supabase/config.toml, supabase/migrations/20260530000001_initial_schema.sql, supabase/migrations/20260530000002_storage_bucket_rls.sql, supabase/migrations/20260530000003_profile_autocreate_trigger.sql, supabase/migrations/20260530000004_phase1_seed_test_trip.sql</files>
  <read_first>
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (Pattern 5: Complete RLS SQL Migration — this is the source of truth for migration 01; Pitfall 1: RLS no policies; Pitfall 6: storage path convention)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md (D-17 profiles table shape; INFRA-02..04 in `<phase_requirements>`)
    - .planning/research/ARCHITECTURE.md (data model + RLS pattern context)
    - .planning/research/PITFALLS.md (Pitfall #2 — RLS off by default)
  </read_first>
  <action>
    Run `npx supabase init` from the `shared-trip` repo root (creates `supabase/` directory + `supabase/config.toml`). Then run `npx supabase link --project-ref {ref}` using the project ref from `.gsd-supabase-project.txt` (created in Plan 01). Linking requires `SUPABASE_ACCESS_TOKEN` env var if running non-TTY; the executor should set it from the user's PAT collected in `user_setup`.

    Write the four migrations:

    **`supabase/migrations/20260530000001_initial_schema.sql`** — COPY VERBATIM the SQL from RESEARCH Pattern 5 (the entire block from `CREATE OR REPLACE FUNCTION public.is_trip_member` through the `CREATE POLICY "Members can add expenses"` block). Do NOT include the storage policies in this file — they go in migration 02. Specifically, include:
    - `is_trip_member(check_trip_id uuid)` function (SECURITY DEFINER, STABLE, `SET search_path = public`)
    - `public.profiles` table + RLS enable + 3 policies (SELECT co-member, UPDATE self, INSERT self)
    - `public.trips` table + RLS enable + 4 policies
    - `public.trip_members` table + RLS enable + 3 policies (SELECT members, INSERT self, DELETE leave-self-or-admin)
    - `public.documents` table + RLS enable + 3 policies (SELECT, INSERT, DELETE)
    - `public.itinerary_items` table + RLS enable + 2 policies (SELECT, ALL FOR members)
    - `public.expenses` table + RLS enable + 2 policies (SELECT, INSERT)
    Every policy must use `(SELECT auth.uid())` (not bare `auth.uid()`) per RESEARCH Anti-Patterns. RLS is enabled in the SAME migration as the table — never split.

    **`supabase/migrations/20260530000002_storage_bucket_rls.sql`** — three policies on `storage.objects` per RESEARCH Pattern 5 tail:
    - `Members can read trip files` (SELECT, bucket_id='trip-documents', `is_trip_member((storage.foldername(name))[1]::uuid)`)
    - `Members can upload trip files` (INSERT, same condition under WITH CHECK)
    - `Authenticated users can delete own uploads` (DELETE, bucket_id='trip-documents', `owner = (SELECT auth.uid())`)
    Add a comment at the top: `-- Requires: bucket 'trip-documents' created in dashboard FIRST (see user_setup). This migration only writes the policies.`

    **`supabase/migrations/20260530000003_profile_autocreate_trigger.sql`** — the `handle_new_user` SECURITY DEFINER function + the `on_auth_user_created` trigger from `<interfaces>`. This ensures every auth.users row (including anonymous ones created by `signInAnonymously()`) gets a paired `profiles` row with NULL `display_name` + NULL `avatar_seed`, so Plan 05's avatar generator falls back to deterministic-from-user-id rendering.

    **`supabase/migrations/20260530000004_phase1_seed_test_trip.sql`** — the seed block from `<interfaces>`. ALL INSERTs are `ON CONFLICT DO NOTHING` so the migration is idempotent. Comment at the top: `-- Phase 1 test data only. Plan 04 will validate /join/{invite_token=22222222...} against this row. Remove during Phase 5 cleanup or before production launch.`

    DO NOT use the Supabase dashboard SQL editor to create tables — every schema mutation in this project MUST go through a tracked migration file. DO NOT split RLS into a separate migration. DO NOT forget the `(SELECT auth.uid())` form. DO NOT use the legacy bare `auth.uid()`.

    DO NOT modify `supabase/config.toml` beyond what `supabase init` produces, except: set `[auth] enable_signup = true` and `[auth.email] enable_signup = true` and `[auth] additional_redirect_urls = ["http://localhost:3000/auth/callback","https://{vercel-domain}/auth/callback"]`. These mirror the dashboard config so local CLI dev mirrors prod.
  </action>
  <verify>
    <automated>test -d supabase/migrations && ls supabase/migrations/*.sql | wc -l | grep -E '^\s*4\s*$' && grep -lc "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260530000001_initial_schema.sql | grep -v '^0$' && ! grep -E '^\s*USING\s*\(\s*auth\.uid\(\)\s*\)' supabase/migrations/*.sql && grep -c 'is_trip_member' supabase/migrations/20260530000001_initial_schema.sql | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly four migration files exist under `supabase/migrations/` with the timestamps `20260530000001..04`.
    - `grep -c 'ENABLE ROW LEVEL SECURITY' supabase/migrations/20260530000001_initial_schema.sql` returns `>=6` (one per table).
    - `grep -cE 'CREATE POLICY' supabase/migrations/20260530000001_initial_schema.sql` returns `>=15` (3 profiles + 4 trips + 3 trip_members + 3 documents + 2 itinerary + 2 expenses = 17).
    - `grep -cE '\(SELECT auth\.uid\(\)\)' supabase/migrations/20260530000001_initial_schema.sql` returns `>=10` (every policy uses the optimized form).
    - `grep -v '^--' supabase/migrations/20260530000001_initial_schema.sql | grep -cE '[^a-z_]auth\.uid\(\)' | tr -d ' '` returns a count whose value matches only inside `(SELECT auth.uid())` wrappers (i.e., bare `auth.uid()` outside `(SELECT ...)` appears 0 times — strict regex variant). A simpler proxy: `grep -E '[^(]auth\.uid\(\)[^)]' supabase/migrations/*.sql | grep -v '^--' | wc -l` should be `0`.
    - `supabase/migrations/20260530000002_storage_bucket_rls.sql` contains `storage.foldername(name))[1]::uuid` (Pitfall 6 enforcement).
    - `supabase/migrations/20260530000003_profile_autocreate_trigger.sql` contains `ON auth.users` AND `SECURITY DEFINER`.
    - `supabase/migrations/20260530000004_phase1_seed_test_trip.sql` contains `22222222-2222-2222-2222-222222222222` (Plan 04 will hit this token).
    - `supabase/config.toml` contains both `localhost:3000/auth/callback` and the Vercel domain in `additional_redirect_urls`.
  </acceptance_criteria>
  <done>Four migration files written with all 6 tables, `is_trip_member()`, every RLS policy in the optimized form, storage RLS, profile autocreate trigger, and the Phase 1 seed trip. Supabase CLI linked to the cloud project.</done>
</task>

<task type="auto" tdd="false" gate="blocking">
  <name>Task 2: [BLOCKING] Push schema migrations to Supabase + generate TypeScript types</name>
  <files>src/types/database.ts</files>
  <read_first>
    - supabase/migrations/*.sql (all four — must exist before push)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (Environment Availability — Supabase CLI install path)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md (the Pitfall 2 warning that RLS-off is the #1 cause of leaks — pushing is the moment of truth)
  </read_first>
  <action>
    **[BLOCKING] This task is mandatory.** The phase cannot pass verification without it. Build and type checks will pass even if the push fails (TS types come from config files, not the live DB) — creating a false-positive verification state.

    Run, in order:
    1. Verify the Supabase Storage bucket `trip-documents` exists (dashboard task in `user_setup`). If absent, FAIL with a clear message — the storage RLS migration (02) references a bucket that does not exist yet. The executor should prompt the user via a `checkpoint:human-action` if the bucket is missing.
    2. Ensure `SUPABASE_ACCESS_TOKEN` env var is set in the shell (non-TTY workaround per phase requirement). If `supabase login` was completed locally, `SUPABASE_ACCESS_TOKEN` may already be available.
    3. Run `npx supabase db push --linked` (uses the link established in Task 1). This applies all four migrations in chronological order.
    4. After push succeeds, regenerate the TypeScript types: `npx supabase gen types typescript --linked --schema public > src/types/database.ts`. The Supabase CLI emits a `Database` interface matching the live schema.
    5. Verify the seed row exists: `npx supabase db remote query "SELECT id, invite_token FROM public.trips WHERE id='11111111-1111-1111-1111-111111111111'"` should return one row.
    6. Verify RLS is actually ON by running, as the unauthenticated `anon` role:
       ```
       curl -sf "${NEXT_PUBLIC_SUPABASE_URL}/rest/v1/trips?select=id" \
         -H "apikey: ${NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY}"
       ```
       Expected: `[]` (empty array — no policy matches the anon role for the seed trip). If this returns the seed row, **RLS is broken** — STOP and investigate before proceeding.

    If the push fails (migration syntax error, FK violation, etc.):
    - Inspect the Supabase error output.
    - Fix the relevant migration file (NEVER edit the live DB directly).
    - Re-run `supabase db push --linked`.

    If push requires interactive prompts that cannot be suppressed (rare on the `--linked` path), the executor MUST stop and surface a `checkpoint:human-action` rather than guessing.
  </action>
  <verify>
    <automated>test -f src/types/database.ts && grep -c 'export.*Database' src/types/database.ts | grep -v '^0$' && grep -c 'trips\|trip_members\|documents\|profiles' src/types/database.ts | grep -v '^0$' && curl -sf "${NEXT_PUBLIC_SUPABASE_URL:-https://placeholder}/rest/v1/trips?select=id" -H "apikey: ${NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY:-anonkey}" 2>/dev/null | grep -E '^\s*\[\s*\]\s*$' && echo "PASS: RLS blocks anon SELECT on trips"</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/database.ts` exists and exports `Database` interface.
    - `grep -cE 'profiles|trips|trip_members|documents|itinerary_items|expenses' src/types/database.ts` returns `>=6` (one match per table).
    - Curl against `/rest/v1/trips` with only the publishable key (no Authorization header) returns `[]` (empty array, NOT 401 — RLS returns empty for unauthenticated; 401 would mean the endpoint is broken). This is the canary that confirms Pitfall #2 is NOT triggered.
    - `npx supabase migration list --linked` shows all four migrations as applied (status column = `applied`).
    - Supabase Dashboard → Storage → bucket `trip-documents` shows Public OFF.
    - `npx supabase db remote query "SELECT count(*) FROM public.trips"` returns `1` (the seed trip).
  </acceptance_criteria>
  <done>All four migrations are applied to the live Supabase project. RLS is ON for all tables and verified empty for unauthenticated callers. `src/types/database.ts` matches the live schema. The Phase 1 seed trip exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Install GitHub Actions keep-alive cron + add secrets + smoke-test a manual run</name>
  <files>.github/workflows/keep-alive.yml, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (Pattern 8: GitHub Actions Keep-Alive Cron — copy the YAML verbatim)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md (INFRA-05 — 5-minute interval is mandatory, NOT 3 days)
    - .planning/research/PITFALLS.md (Pitfall #4 free-tier pause)
  </read_first>
  <action>
    Write `.github/workflows/keep-alive.yml` exactly per RESEARCH Pattern 8 — cron `*/5 * * * *`, single curl step against `${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/` with the apikey header set from `${{ secrets.SUPABASE_PUBLISHABLE_KEY }}`. Include `workflow_dispatch:` so manual smoke-test is possible.

    Add the two GitHub repo secrets (the user must add via `gh secret set` — the executor can run these, the credentials come from the local `.env.local`):
    - `gh secret set NEXT_PUBLIC_SUPABASE_URL --body "$(grep NEXT_PUBLIC_SUPABASE_URL .env.local | cut -d= -f2)"`
    - `gh secret set SUPABASE_PUBLISHABLE_KEY --body "$(grep NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY .env.local | cut -d= -f2)"`

    Trigger a manual run: `gh workflow run keep-alive.yml` then `gh run list --workflow=keep-alive.yml --limit 1` and `gh run view {run-id} --log` to confirm the curl returned successfully.

    DO NOT add `SUPABASE_SECRET_KEY` as a GitHub Actions secret — keep-alive uses the publishable key only (anti-pattern: never expose the service role outside Vercel server runtime).

    Update `.gitignore` to ensure `.env.local`, `.gsd-*.txt`, `supabase/.temp/`, `supabase/.branches/`, `.vercel/`, `node_modules/`, `.next/` are all ignored. Append if missing.
  </action>
  <verify>
    <automated>test -f .github/workflows/keep-alive.yml && grep -c '\*/5 \* \* \* \*' .github/workflows/keep-alive.yml | grep -v '^0$' && gh secret list 2>/dev/null | grep -c 'NEXT_PUBLIC_SUPABASE_URL\|SUPABASE_PUBLISHABLE_KEY' | grep -v '^0$' && gh run list --workflow=keep-alive.yml --limit 1 --json status -q '.[0].status' | grep -E 'completed|in_progress'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/keep-alive.yml` exists with cron `*/5 * * * *` AND `workflow_dispatch:` AND a single curl step.
    - `gh secret list` shows `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_PUBLISHABLE_KEY` as repo secrets.
    - `gh secret list` does NOT show `SUPABASE_SECRET_KEY` (service role role anti-pattern).
    - One manual workflow run via `workflow_dispatch` completed with status `completed` and conclusion `success`.
    - `gh run view {run-id} --log | grep -c 'HTTP/'` returns `>=1` (curl made the request).
    - `.gitignore` contains `.env.local`, `supabase/.temp/`, `.vercel/`.
  </acceptance_criteria>
  <done>Keep-alive workflow is committed, GitHub secrets are configured, a manual smoke-test run completed successfully. The cron will fire automatically every 5 minutes thereafter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify schema, RLS, storage bucket, and keep-alive cron are live</name>
  <what-built>
    Database schema applied (6 tables + RLS + is_trip_member + storage policies + profile autocreate trigger + seed trip). TypeScript types generated. GitHub Actions keep-alive workflow installed and smoke-tested.
  </what-built>
  <how-to-verify>
    Run the following commands and report results:

    1. **RLS is on (the most important verification):**
       ```
       curl -sf "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/trips?select=id" -H "apikey: $NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY"
       ```
       Expected output: `[]` (empty JSON array). If you see the seed trip row, RLS is broken — Pitfall #2 has been triggered.

    2. **Seed trip exists (server-side via Supabase Dashboard SQL editor):**
       Open Supabase Dashboard → SQL Editor → run:
       ```sql
       SELECT id, name, invite_token FROM public.trips;
       ```
       Expected: one row with `id=11111111-...` and `invite_token=22222222-...`.

    3. **Storage bucket exists:**
       Open Supabase Dashboard → Storage. Confirm bucket `trip-documents` exists, is PRIVATE (Public toggle OFF), and has three policies under Policies tab.

    4. **Profile autocreate trigger:**
       In Dashboard → SQL Editor:
       ```sql
       SELECT tgname FROM pg_trigger WHERE tgname='on_auth_user_created';
       ```
       Expected: one row.

    5. **Keep-alive cron:**
       Open `https://github.com/{repo}/actions` in a browser. Confirm `Supabase Keep-Alive` workflow exists and the most recent run has status "Success".

    6. **TS types match:**
       Run `npx tsc --noEmit` in the `shared-trip` project. Expected: exits 0.

    If ANY of these fails, describe which one and what the actual output was. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/SDK → Postgres (via PostgREST) | Untrusted client claims attached as JWT; trust enforced by RLS. |
| Browser/SDK → Storage (via storage.objects) | Same JWT; trust enforced by storage RLS on `storage.objects`. |
| Migration file → live database | Schema mutations happen ONLY via tracked migrations — never via dashboard SQL editor. |
| GitHub Actions → Supabase REST | Uses publishable key (low-privilege) only; service role key is NEVER in GitHub Actions secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | A user accessing another user's trip data via the anon publishable key | mitigate | Every table has `ENABLE ROW LEVEL SECURITY` in the SAME migration that creates it (Pattern 5). Every per-trip policy uses `is_trip_member(trip_id)`. Task 2 verifies via curl that anon callers get `[]`. |
| T-02-02 | Information Disclosure | RLS-on-but-no-policies returns empty silently → developer disables RLS to "fix" | mitigate | Migration 01 includes at minimum SELECT/INSERT policies on every table at the moment RLS is enabled. There is no window where a table has RLS-on with zero policies. (Pitfall 1) |
| T-02-03 | Tampering | An anonymous user inserts a `trip_members` row for someone else's trip | mitigate | `trip_members` INSERT policy has `WITH CHECK (user_id = (SELECT auth.uid()))` — a user can only insert their OWN membership. Combined with the `/join/[token]` flow (Plan 04) requiring possession of the invite token. |
| T-02-04 | Information Disclosure | Storage path traversal (`../../../`) to read another trip's files | mitigate | Storage RLS policy extracts the first folder segment via `storage.foldername(name))[1]::uuid` and casts to UUID. Invalid UUIDs fail the cast → policy returns false → blocked. Plan 03/Phase 3 must enforce path convention `{tripId}/{filename}` in upload code (Pitfall 6). |
| T-02-05 | Elevation of Privilege | `is_trip_member()` SQL-injection via crafted `check_trip_id` value | accept | Function parameter is typed `uuid`; Postgres rejects non-UUID values at the type boundary before the function body executes. Function body uses only the parameter inside a parameterized `WHERE` — no dynamic SQL construction. `SET search_path = public` prevents schema-injection. |
| T-02-06 | Information Disclosure | Service role key leaked via the keep-alive cron (false positive — cron uses publishable key) | mitigate | Workflow YAML uses `${{ secrets.SUPABASE_PUBLISHABLE_KEY }}` only. Task 3 acceptance criteria explicitly check `gh secret list` does NOT contain `SUPABASE_SECRET_KEY`. |
| T-02-07 | Repudiation | Migrations applied via dashboard SQL editor leave no Git-trackable record | mitigate | All schema changes go through `supabase/migrations/*.sql` (Git-tracked). `supabase migration list --linked` confirms applied migrations match the repo. Phase 5 may add a CI check that fails if the local migration set diverges from the live DB. |
| T-02-SC | Tampering | Supply-chain: `supabase` CLI dev dep legitimacy | mitigate | Already verified in Plan 01 Package Legitimacy Audit (`supabase` CLI is Supabase-official). Task 2 explicitly does NOT add new packages — uses the CLI installed in Plan 01. No legitimacy checkpoint needed here. |
</threat_model>

<verification>
- All four migrations applied to live Supabase project (confirmed via `supabase migration list --linked`).
- Anon curl against `/rest/v1/trips` returns `[]` — Pitfall #2 is NOT triggered.
- Storage bucket `trip-documents` exists and is private.
- `src/types/database.ts` exists and `tsc --noEmit` exits 0.
- GitHub Actions keep-alive workflow exists; one manual run completed successfully.
- Schema-related files committed: `git status` clean except `.env.local`.
</verification>

<success_criteria>
- INFRA-02: schema deployed with `trips`, `trip_members`, `documents`, `itinerary_items`, `expenses`, `profiles`.
- INFRA-03: RLS enabled on every table + `is_trip_member()` function exists + verified empty for anon caller.
- INFRA-04: `trip-documents` bucket exists, private, with RLS on `storage.objects`.
- INFRA-05: GitHub Actions cron firing every 5 minutes; one successful manual run logged.
- Seed trip + invite token `22222222-2222-2222-2222-222222222222` exists so Plan 04 can validate `/join/[token]`.
- TypeScript types match live schema; downstream plans can `import { Database } from '@/types/database'`.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`. Include: the seed `trip_id` + `invite_token` (so Plan 04 has them in one place), the migration filenames + timestamps, and confirmation that storage RLS is active.
</output>
